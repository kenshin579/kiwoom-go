// Code generated by tools/gen. DO NOT EDIT.

package account

import (
	"context"

	"github.com/kenshin579/kiwoom-go/internal/transport"
)

// GetDomesticAccountEvaluationStatusRequest 는 계좌평가현황요청(kt00004) 요청이다.
type GetDomesticAccountEvaluationStatusRequest struct {
	// 상장폐지조회구분 (필수, 1자, 0:전체, 1:상장폐지종목제외)
	QryTp string `json:"qry_tp"`
	// 국내거래소구분 (필수, 6자, KRX:한국거래소,NXT:넥스트트레이드)
	DmstStexTp string `json:"dmst_stex_tp"`
}

// GetDomesticAccountEvaluationStatusResponse 는 계좌평가현황요청(kt00004) 응답이다.
type GetDomesticAccountEvaluationStatusResponse struct {
	// 계좌명 (30자)
	AcntNm string `json:"acnt_nm"`
	// 지점명 (30자)
	BrchNm string `json:"brch_nm"`
	// 예수금 (12자, 단위: 원, 좌측 0-padding 처리된 부호 포함 12자리 숫자)
	Entr string `json:"entr"`
	// D+2추정예수금 (12자, 단위: 원, 좌측 0-padding 처리된 부호 포함 12자리 숫자)
	D2Entra string `json:"d2_entra"`
	// 유가잔고평가액 (12자, 단위: 원, 좌측 0-padding 처리된 부호 포함 12자리 숫자)
	TotEstAmt string `json:"tot_est_amt"`
	// 예탁자산평가액 (12자, 단위: 원, 좌측 0-padding 처리된 부호 포함 12자리 숫자)
	AsetEvltAmt string `json:"aset_evlt_amt"`
	// 총매입금액 (12자, 단위: 원, 좌측 0-padding 처리된 부호 포함 12자리 숫자)
	TotPurAmt string `json:"tot_pur_amt"`
	// 추정예탁자산 (12자, 단위: 원, 좌측 0-padding 처리된 부호 포함 12자리 숫자)
	PrsmDpstAsetAmt string `json:"prsm_dpst_aset_amt"`
	// 매도담보대출금 (12자, 단위: 원, 좌측 0-padding 처리된 부호 포함 12자리 숫자)
	TotGrntSella string `json:"tot_grnt_sella"`
	// 당일투자원금 (12자, 단위: 원, 좌측 0-padding 처리된 부호 포함 12자리 숫자)
	TdyLspftAmt string `json:"tdy_lspft_amt"`
	// 당월투자원금 (12자, 단위: 원, 좌측 0-padding 처리된 부호 포함 12자리 숫자)
	InvtBsamt string `json:"invt_bsamt"`
	// 누적투자원금 (12자, 단위: 원, 좌측 0-padding 처리된 부호 포함 12자리 숫자)
	LspftAmt string `json:"lspft_amt"`
	// 당일투자손익 (12자, 단위: 원, 좌측 0-padding 처리된 부호 포함 12자리 숫자)
	TdyLspft string `json:"tdy_lspft"`
	// 당월투자손익 (12자, 단위: 원, 좌측 0-padding 처리된 부호 포함 12자리 숫자)
	Lspft2 string `json:"lspft2"`
	// 누적투자손익 (12자, 단위: 원, 좌측 0-padding 처리된 부호 포함 12자리 숫자)
	Lspft string `json:"lspft"`
	// 당일손익율 (12자, 단위: %, 소수점 둘째 자리까지 포맷된 백분율)
	TdyLspftRt string `json:"tdy_lspft_rt"`
	// 당월손익율 (12자, 단위: %, 소수점 둘째 자리까지 포맷된 백분율)
	LspftRatio string `json:"lspft_ratio"`
	// 누적손익율 (12자, 단위: %, 소수점 둘째 자리까지 포맷된 백분율)
	LspftRt string `json:"lspft_rt"`
	// 종목별계좌평가현황
	StkAcntEvltPrst []GetDomesticAccountEvaluationStatusStkAcntEvltPrstItem `json:"stk_acnt_evlt_prst"`
}

// GetDomesticAccountEvaluationStatusStkAcntEvltPrstItem 는 StkAcntEvltPrst 의 원소다.
type GetDomesticAccountEvaluationStatusStkAcntEvltPrstItem struct {
	// 종목코드 (12자, 접두어 1자리 + 종목코드 6자리, 접두어(A: 주식 / J: ELW / Q: ETN))
	StkCd string `json:"stk_cd"`
	// 종목명 (30자)
	StkNm string `json:"stk_nm"`
	// 보유수량 (12자, 단위: 1주, 좌측 0-padding 처리된 부호 포함 12자리 숫자)
	RmndQty string `json:"rmnd_qty"`
	// 평균단가 (12자, 단위: 원, 좌측 0-padding 처리된 부호 포함 12자리 숫자)
	AvgPrc string `json:"avg_prc"`
	// 현재가 (12자, 단위: 원, 좌측 0-padding 처리된 부호 포함 12자리 숫자)
	CurPrc string `json:"cur_prc"`
	// 평가금액 (12자, 단위: 원, 좌측 0-padding 처리된 부호 포함 12자리 숫자)
	EvltAmt string `json:"evlt_amt"`
	// 손익금액 (12자, 단위: 원, 좌측 0-padding 처리된 부호 포함 12자리 숫자)
	PlAmt string `json:"pl_amt"`
	// 손익율 (12자, 단위: %, 소수점 넷째 자리까지 포맷된 백분율)
	PlRt string `json:"pl_rt"`
	// 대출일 (10자, YYYYMMDD)
	LoanDt string `json:"loan_dt"`
	// 매입금액 (12자, 단위: 원, 좌측 0-padding 처리된 부호 포함 12자리 숫자)
	PurAmt string `json:"pur_amt"`
	// 결제잔고 (12자, 단위: 1주, 좌측 0-padding 처리된 부호 포함 12자리 숫자)
	SetlRemn string `json:"setl_remn"`
	// 전일매수수량 (12자, 단위: 1주, 좌측 0-padding 처리된 부호 포함 12자리 숫자)
	PredBuyq string `json:"pred_buyq"`
	// 전일매도수량 (12자, 단위: 1주, 좌측 0-padding 처리된 부호 포함 12자리 숫자)
	PredSellq string `json:"pred_sellq"`
	// 금일매수수량 (12자, 단위: 1주, 좌측 0-padding 처리된 부호 포함 12자리 숫자)
	TdyBuyq string `json:"tdy_buyq"`
	// 금일매도수량 (12자, 단위: 1주, 좌측 0-padding 처리된 부호 포함 12자리 숫자)
	TdySellq string `json:"tdy_sellq"`
}

// GetDomesticAccountEvaluationStatus 는 계좌평가현황요청(kt00004) 이다.
//
// 메뉴: 국내주식 > 계좌 > 계좌평가현황요청(kt00004)
// URL:  /api/dostk/acnt
func (c *Client) GetDomesticAccountEvaluationStatus(ctx context.Context, req GetDomesticAccountEvaluationStatusRequest, opts ...transport.Option) (*GetDomesticAccountEvaluationStatusResponse, transport.Meta, error) {
	var out GetDomesticAccountEvaluationStatusResponse
	meta, err := c.http.Do(ctx, transport.Request{
		APIID: "kt00004",
		Path:  "/api/dostk/acnt",
		Body:  req,
	}, &out, opts...)
	if err != nil {
		return nil, meta, err
	}
	return &out, meta, nil
}
