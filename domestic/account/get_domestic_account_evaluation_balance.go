// Code generated by tools/gen. DO NOT EDIT.

package account

import (
	"context"

	"github.com/kenshin579/kiwoom-go/internal/transport"
)

// GetDomesticAccountEvaluationBalanceRequest 는 계좌평가잔고내역요청(kt00018) 요청이다.
type GetDomesticAccountEvaluationBalanceRequest struct {
	// 조회구분 (필수, 1자, 1:합산, 2:개별)
	QryTp string `json:"qry_tp"`
	// 국내거래소구분 (필수, 6자, KRX:한국거래소,NXT:넥스트트레이드)
	DmstStexTp string `json:"dmst_stex_tp"`
}

// GetDomesticAccountEvaluationBalanceResponse 는 계좌평가잔고내역요청(kt00018) 응답이다.
type GetDomesticAccountEvaluationBalanceResponse struct {
	// 총매입금액 (15자, 단위: 원, 좌측 0-padding 처리된 부호 포함 15자리 숫자)
	TotPurAmt string `json:"tot_pur_amt"`
	// 총평가금액 (15자, 단위: 원, 좌측 0-padding 처리된 부호 포함 15자리 숫자)
	TotEvltAmt string `json:"tot_evlt_amt"`
	// 총평가손익금액 (15자, 단위: 원, 좌측 0-padding 처리된 부호 포함 15자리 숫자)
	TotEvltPl string `json:"tot_evlt_pl"`
	// 총수익률(%) (12자, 단위: %, 소수점 둘째 자리까지 포맷된 백분율)
	TotPrftRt string `json:"tot_prft_rt"`
	// 추정예탁자산 (15자, 단위: 원, 좌측 0-padding 처리된 부호 포함 15자리 숫자)
	PrsmDpstAsetAmt string `json:"prsm_dpst_aset_amt"`
	// 총대출금 (15자, 단위: 원, 좌측 0-padding 처리된 부호 포함 15자리 숫자)
	TotLoanAmt string `json:"tot_loan_amt"`
	// 총융자금액 (15자, 단위: 원, 좌측 0-padding 처리된 부호 포함 15자리 숫자)
	TotCrdLoanAmt string `json:"tot_crd_loan_amt"`
	// 총대주금액 (15자, 단위: 원, 좌측 0-padding 처리된 부호 포함 15자리 숫자)
	TotCrdLsAmt string `json:"tot_crd_ls_amt"`
	// 계좌평가잔고개별합산
	AcntEvltRemnIndvTot []GetDomesticAccountEvaluationBalanceAcntEvltRemnIndvTotItem `json:"acnt_evlt_remn_indv_tot"`
}

// GetDomesticAccountEvaluationBalanceAcntEvltRemnIndvTotItem 는 AcntEvltRemnIndvTot 의 원소다.
type GetDomesticAccountEvaluationBalanceAcntEvltRemnIndvTotItem struct {
	// 종목번호 (12자, 접두어 1자리 + 종목코드 6자리, 접두어(A: 주식 / J: ELW / Q: ETN))
	StkCd string `json:"stk_cd"`
	// 종목명 (40자)
	StkNm string `json:"stk_nm"`
	// 평가손익 (15자, 단위: 원, 좌측 0-padding 처리된 부호 포함 15자리 숫자)
	EvltvPrft string `json:"evltv_prft"`
	// 수익률(%) (12자, 단위: %, 소수점 둘째 자리까지 포맷된 백분율)
	PrftRt string `json:"prft_rt"`
	// 매입가 (15자, 단위: 원, 좌측 0-padding 처리된 부호 포함 15자리 숫자)
	PurPric string `json:"pur_pric"`
	// 전일종가 (12자, 단위: 원, 좌측 0-padding 처리된 부호 포함 12자리 숫자)
	PredClosePric string `json:"pred_close_pric"`
	// 보유수량 (15자, 단위: 1주, 좌측 0-padding 처리된 부호 포함 15자리 숫자)
	RmndQty string `json:"rmnd_qty"`
	// 매매가능수량 (15자, 단위: 1주, 좌측 0-padding 처리된 부호 포함 15자리 숫자)
	TrdeAbleQty string `json:"trde_able_qty"`
	// 현재가 (12자, 단위: 원, 좌측 0-padding 처리된 부호 포함 12자리 숫자)
	CurPrc string `json:"cur_prc"`
	// 전일매수수량 (15자, 단위: 1주, 좌측 0-padding 처리된 부호 포함 15자리 숫자)
	PredBuyq string `json:"pred_buyq"`
	// 전일매도수량 (15자, 단위: 1주, 좌측 0-padding 처리된 부호 포함 15자리 숫자)
	PredSellq string `json:"pred_sellq"`
	// 금일매수수량 (15자, 단위: 1주, 좌측 0-padding 처리된 부호 포함 15자리 숫자)
	TdyBuyq string `json:"tdy_buyq"`
	// 금일매도수량 (15자, 단위: 1주, 좌측 0-padding 처리된 부호 포함 15자리 숫자)
	TdySellq string `json:"tdy_sellq"`
	// 매입금액 (15자, 단위: 원, 좌측 0-padding 처리된 부호 포함 15자리 숫자)
	PurAmt string `json:"pur_amt"`
	// 매입수수료 (15자, 단위: 원, 좌측 0-padding 처리된 부호 포함 15자리 숫자)
	PurCmsn string `json:"pur_cmsn"`
	// 평가금액 (15자, 단위: 원, 좌측 0-padding 처리된 부호 포함 15자리 숫자)
	EvltAmt string `json:"evlt_amt"`
	// 평가수수료 (15자, 단위: 원, 좌측 0-padding 처리된 부호 포함 15자리 숫자)
	SellCmsn string `json:"sell_cmsn"`
	// 세금 (15자, 단위: 원, 좌측 0-padding 처리된 부호 포함 15자리 숫자)
	Tax string `json:"tax"`
	// 수수료합 (15자, 매입수수료 + 평가수수료)
	SumCmsn string `json:"sum_cmsn"`
	// 보유비중(%) (12자, 단위: %, 소수점 둘째 자리까지 포맷된 백분율)
	PossRt string `json:"poss_rt"`
	// 신용구분 (2자)
	CrdTp string `json:"crd_tp"`
	// 신용구분명 (4자)
	CrdTpNm string `json:"crd_tp_nm"`
	// 대출일 (8자, YYYYMMDD)
	CrdLoanDt string `json:"crd_loan_dt"`
}

// GetDomesticAccountEvaluationBalance 는 계좌평가잔고내역요청(kt00018) 이다.
//
// 메뉴: 국내주식 > 계좌 > 계좌평가잔고내역요청(kt00018)
// URL:  /api/dostk/acnt
func (c *Client) GetDomesticAccountEvaluationBalance(ctx context.Context, req GetDomesticAccountEvaluationBalanceRequest, opts ...transport.Option) (*GetDomesticAccountEvaluationBalanceResponse, transport.Meta, error) {
	var out GetDomesticAccountEvaluationBalanceResponse
	meta, err := c.http.Do(ctx, transport.Request{
		APIID: "kt00018",
		Path:  "/api/dostk/acnt",
		Body:  req,
	}, &out, opts...)
	if err != nil {
		return nil, meta, err
	}
	return &out, meta, nil
}
