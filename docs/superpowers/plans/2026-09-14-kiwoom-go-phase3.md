# kiwoom-go 3단계 — 실시간 WebSocket 23 + 조건검색 8 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 남은 31개(실시간 23 · 조건검색 8)를 덮어 스펙 337개를 전부 지원한다.

**Architecture:** `internal/wstransport` 가 경로별 WebSocket 연결 하나를 맡아 로그인·PING·멀티플렉싱·재연결을 감춘다. 실시간 값은 FID 표(`tools/gen/fids.go`, 손으로 적는다)를 거쳐 타입별 구조체로 생성되고, 사용자는 타입이 살아 있는 채널로 받는다. 조건검색 7개는 REST 처럼 생성하고 `ka10173` 만 손으로 쓴다.

**Tech Stack:** Go 1.25 · `github.com/coder/websocket` v1.8.14(형제 라이브러리 `toss-go` 와 같은 선택) · `text/template` 코드 생성 · `httptest` 가짜 WS 서버

**설계:** `docs/superpowers/specs/2026-09-14-kiwoom-go-phase3-design.md`

---

## 모든 작업자가 먼저 읽을 것

### 커밋 훅

`main`/`master`/`dev` 직접 커밋을 막는 훅이 있고 **셸의 현재 작업 디렉터리** 기준으로 판정한다.

1. 첫 명령으로 `cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go` **만** 실행해 디렉터리를 고정한다.
2. 이후 `git commit` 명령에 `cd` 를 **다시 붙이지 않는다** — 붙이면 명령 전체가 차단되어 파일 수정까지 통째로 날아간다.
3. `git add -A` / `git add .` / `git commit -a` 금지. **항상 경로 명시.**

브랜치는 `feature/phase3-realtime` 이다. 전환하지 않는다.

### 두 개의 모듈

`tools/` 는 **별도 Go 모듈**이다. 생성기 관련 `go` 명령은 `cd tools` 후에, 라이브러리 관련은 저장소 루트에서 돌린다.

### 생성물을 손으로 고치지 않는다

`// Code generated by tools/gen. DO NOT EDIT.` 헤더가 붙은 파일은 손대지 않는다. 잘못되면 **생성기를 고치고 다시 돌린다.** `tools/gen/golden_test.go` 가 이 규율을 강제한다(손으로 고치면 mismatch 로 실패한다).

### 확정된 WS 규약 (설계 §3·§7에서 공식 클라이언트로 확인함)

```
1. wss://<host>:10000 + <path> 로 연결
2. 송신: {"trnm":"LOGIN","token":"<접근토큰>"}
3. 수신: trnm=="LOGIN" 인 메시지. return_code != 0 이면 실패.
        로그인 응답 전에 다른 메시지가 오면 에러.
4. trnm=="PING" 인 메시지(또는 "PING" 문자열)는 받은 것을 그대로 echo 하고
   호출자에게 올리지 않는다.
5. 등록: {"trnm":"REG","grp_no":"1","refresh":"1",
          "data":[{"item":["005930"],"type":["0B"]}]}      ← item·type 이 배열
6. 푸시: {"trnm":"REAL","data":[{"type":"0B","name":"주식체결",
          "item":"005930","values":{"10":"-82000"}}]}      ← item 이 스칼라, values 는 맵
```

---

## 파일 구조

| 파일 | 책임 |
|---|---|
| `internal/wstransport/conn.go` | 연결 하나 — 다이얼·로그인·PING·수신 루프 |
| `internal/wstransport/request.go` | 요청/응답 왕복(`trnm` 으로 짝짓기). 조건검색이 쓴다 |
| `internal/wstransport/sub.go` | 구독 등록·해지·라우팅·느린 소비자 |
| `internal/wstransport/reconnect.go` | 재연결·재구독·`GapError` |
| `internal/wstransport/errors.go` | `GapError`·`SlowConsumerError`·`LoginError` |
| `internal/wstransport/*_test.go` | 가짜 WS 서버 기반 테스트 |
| `stream.go` (루트) | `Event[T]`·에러 타입을 외부에 내보내는 별칭 |
| `config.go` (루트) | WS URL 상수와 옵션 추가 |
| `tools/gen/fids.go` | **손으로 적는** FID → Go 이름 표 400개 |
| `tools/gen/render_realtime.go` | 실시간 값 구조체·구독 메서드 템플릿 |
| `tools/gen/render_condition.go` | 조건검색 요청/응답 템플릿 |
| `domestic/realtime/`, `overseas/realtime/` | 생성물 — 값 구조체 23벌 + 구독 메서드 |
| `domestic/condition/`, `overseas/condition/` | 생성물 6개 + 손으로 쓴 `ka10173`·`usa20290` |

---

## Task 1: 확인한 WS 규약을 SOURCE.md 에 적는다

핸드셰이크는 스펙에 없다(`LOGIN`·`PING` 0회). 공식 Python 클라이언트에서 읽어 확인했다. **어디서 왔는지를 남기지 않으면 다음 사람이 같은 조사를 다시 한다.**

**Files:**
- Modify: `tools/spec/SOURCE.md`

- [ ] **Step 1: 현재 파일을 읽는다**

```bash
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go
cat tools/spec/SOURCE.md
```

- [ ] **Step 2: 맨 끝에 절을 덧붙인다**

`## 갱신하는 법` 절 **뒤에** 아래를 통째로 붙인다:

````markdown
## WebSocket 규약 (스펙에 없어 예제에서 확인함)

`kiwoom_api_spec.json` 은 api-id 별 메시지만 담는다. 연결을 여는 절차는 없다 —
`LOGIN`·`PING`·`PONG` 이 스펙 전체에서 **0회**다.

아래는 같은 커밋(`234560d213acd8871ae344b5481aecd2f30287fa`)의 공식 Python 클라이언트에서
읽은 것이다. **코드를 옮겨 오지 않았다. 규약만 적었다.**

| 사실 | 출처 |
|---|---|
| 로그인 패킷 `{"trnm":"LOGIN","token":…}` | `kiwoom/core/ws_client.py` `_login` |
| 로그인 응답을 먼저 받아야 하고 `return_code != 0` 이면 실패 | 같은 파일 `_await_login_ack` |
| PING 은 **받은 것을 그대로 echo** | 같은 파일 `_receive_non_ping_message` |
| 인증 실패 시 토큰 재발급 후 **로그인만** 재시도 | 같은 파일 `connect(retry_on_auth_failure=True)` |
| REG 패킷의 `item`·`type` 이 **배열** | `examples/국내주식/실시간시세/subscribe_domestic_stock_trade_async.py` |
| REAL 푸시의 `item` 은 **스칼라**, `values` 는 **맵** | `kiwoom/realtime/decoders.py` 머리말 |
| `usa20290` 은 푸시가 오지만 FID 표가 비어 있음(`COLUMNS = {}`) | `examples/미국주식/조건검색/request_overseas_realtime_condition_search_async.py` |
| 미국 조건검색 `trnm` 은 `GCNSRREQ` | 같은 파일 |

스펙과 **어긋나는** 곳(이 표가 이긴다):

- 스펙은 REG 의 `item`·`type` 을 depth-1 `String` 으로 적었지만 실제로는 배열이다.
- 스펙은 `values` 를 실시간 23곳에서 `LIST`, `ka10173` 에서 `Object` 로 적었다.
  실제는 **맵**이다. `LIST` 쪽이 문서 오류다.

규약을 다시 확인하려면 위 파일들을 같은 커밋에서 읽어라. 원격을 빌드 때 받아오지 않는
이유는 스펙과 같다 — 재현 가능해야 하고, 바뀌면 의도적인 갱신 커밋으로 드러나야 한다.
````

- [ ] **Step 3: 커밋**

```bash
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go
git add tools/spec/SOURCE.md
git commit -m "docs: 공식 예제에서 확인한 WebSocket 규약 기록" -- tools/spec/SOURCE.md
```

---

## Task 2: `internal/wstransport` — 연결·로그인·PING

**Files:**
- Create: `internal/wstransport/errors.go`
- Create: `internal/wstransport/conn.go`
- Create: `internal/wstransport/fake_test.go`
- Create: `internal/wstransport/conn_test.go`
- Modify: `go.mod`

- [ ] **Step 1: 의존성을 넣는다**

```bash
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go
go get github.com/coder/websocket@v1.8.14
```

형제 라이브러리 `toss-go` 와 같은 버전이다. 워크스페이스에서 이미 쓰는 것을 따른다.

- [ ] **Step 2: 가짜 WS 서버를 쓴다 (테스트 도구)**

`internal/wstransport/fake_test.go`:

```go
package wstransport

import (
	"context"
	"encoding/json"
	"net/http"
	"net/http/httptest"
	"strings"
	"sync"
	"testing"
	"time"

	"github.com/coder/websocket"
)

// fakeServer 는 키움 WS 서버 흉내다.
//
// 실서버 없이 로그인·PING·등록·재연결을 전부 검증하려면 이게 두꺼워야 한다.
// 모의투자가 실시간을 주는지 확인되지 않았으므로(설계 §11), 당분간 이것이 유일한 그물이다.
type fakeServer struct {
	*httptest.Server

	mu       sync.Mutex
	received []map[string]any // 서버가 받은 패킷 전부(로그인 포함)
	conns    int              // 몇 번 연결됐나 — 재연결 검증용

	// loginCode 는 로그인 응답의 return_code. 0 이 정상.
	loginCode int
	// onConn 은 연결이 열릴 때마다 불린다. 푸시를 밀어 넣거나 끊을 때 쓴다.
	onConn func(t *testing.T, n int, c *websocket.Conn)
}

func newFakeServer(t *testing.T) *fakeServer {
	t.Helper()
	f := &fakeServer{}
	f.Server = httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		c, err := websocket.Accept(w, r, nil)
		if err != nil {
			return
		}
		defer func() { _ = c.CloseNow() }()

		f.mu.Lock()
		f.conns++
		n := f.conns
		f.mu.Unlock()

		// 로그인 패킷을 받아 응답한다.
		var login map[string]any
		if err := f.readJSON(c, &login); err != nil {
			return
		}
		f.record(login)
		_ = f.writeJSON(c, map[string]any{
			"trnm": "LOGIN", "return_code": f.loginCode, "return_msg": "",
		})

		if f.onConn != nil {
			f.onConn(t, n, c)
		}

		// 남은 시간 동안 받은 패킷을 기록만 한다.
		for {
			var m map[string]any
			if err := f.readJSON(c, &m); err != nil {
				return
			}
			f.record(m)
		}
	}))
	t.Cleanup(f.Close)
	return f
}

// wsURL 은 httptest 의 http:// 를 ws:// 로 바꾼 것이다.
func (f *fakeServer) wsURL() string {
	return strings.Replace(f.URL, "http://", "ws://", 1)
}

func (f *fakeServer) record(m map[string]any) {
	f.mu.Lock()
	defer f.mu.Unlock()
	f.received = append(f.received, m)
}

// packets 는 지금까지 받은 패킷을 복사해 돌려준다.
func (f *fakeServer) packets() []map[string]any {
	f.mu.Lock()
	defer f.mu.Unlock()
	return append([]map[string]any(nil), f.received...)
}

func (f *fakeServer) connCount() int {
	f.mu.Lock()
	defer f.mu.Unlock()
	return f.conns
}

func (f *fakeServer) readJSON(c *websocket.Conn, v any) error {
	ctx, cancel := context.WithTimeout(context.Background(), 5*time.Second)
	defer cancel()
	_, b, err := c.Read(ctx)
	if err != nil {
		return err
	}
	return json.Unmarshal(b, v)
}

func (f *fakeServer) writeJSON(c *websocket.Conn, v any) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}
	ctx, cancel := context.WithTimeout(context.Background(), 5*time.Second)
	defer cancel()
	return c.Write(ctx, websocket.MessageText, b)
}

// stubToken 은 고정 토큰을 주는 TokenSource 다.
type stubToken struct {
	mu          sync.Mutex
	token       string
	invalidated int
}

func (s *stubToken) Token(context.Context) (string, error) {
	s.mu.Lock()
	defer s.mu.Unlock()
	return s.token, nil
}

func (s *stubToken) Invalidate() {
	s.mu.Lock()
	defer s.mu.Unlock()
	s.invalidated++
}

func (s *stubToken) invalidateCount() int {
	s.mu.Lock()
	defer s.mu.Unlock()
	return s.invalidated
}
```

- [ ] **Step 3: 실패하는 테스트를 쓴다**

`internal/wstransport/conn_test.go`:

```go
package wstransport

import (
	"context"
	"testing"
	"time"

	"github.com/coder/websocket"
)

func TestConn_로그인_패킷을_먼저_보낸다(t *testing.T) {
	f := newFakeServer(t)
	tok := &stubToken{token: "TKN"}

	c := New(f.wsURL(), "", tok)
	ctx, cancel := context.WithTimeout(context.Background(), 5*time.Second)
	defer cancel()

	if err := c.Connect(ctx); err != nil {
		t.Fatalf("Connect: %v", err)
	}
	defer func() { _ = c.Close() }()

	ps := f.packets()
	if len(ps) == 0 {
		t.Fatal("서버가 아무 패킷도 못 받았다")
	}
	if ps[0]["trnm"] != "LOGIN" {
		t.Errorf("첫 패킷 trnm = %v, 기대 LOGIN", ps[0]["trnm"])
	}
	if ps[0]["token"] != "TKN" {
		t.Errorf("token = %v, 기대 TKN", ps[0]["token"])
	}
}

func TestConn_로그인_실패는_에러다(t *testing.T) {
	f := newFakeServer(t)
	f.loginCode = 3 // 0 이 아니면 실패

	c := New(f.wsURL(), "", &stubToken{token: "TKN"})
	ctx, cancel := context.WithTimeout(context.Background(), 5*time.Second)
	defer cancel()

	err := c.Connect(ctx)
	if err == nil {
		t.Fatal("로그인 실패인데 에러가 없다")
	}
	var le *LoginError
	if !errorsAs(err, &le) {
		t.Fatalf("에러 타입 = %T, 기대 *LoginError", err)
	}
	if le.ReturnCode != 3 {
		t.Errorf("ReturnCode = %d, 기대 3", le.ReturnCode)
	}
}

func TestConn_PING_을_그대로_되돌려_보낸다(t *testing.T) {
	f := newFakeServer(t)
	f.onConn = func(t *testing.T, n int, c *websocket.Conn) {
		_ = f.writeJSON(c, map[string]any{"trnm": "PING", "nonce": "abc"})
	}

	c := New(f.wsURL(), "", &stubToken{token: "TKN"})
	ctx, cancel := context.WithTimeout(context.Background(), 5*time.Second)
	defer cancel()
	if err := c.Connect(ctx); err != nil {
		t.Fatalf("Connect: %v", err)
	}
	defer func() { _ = c.Close() }()

	// PING echo 가 서버에 도착할 때까지 기다린다.
	deadline := time.Now().Add(3 * time.Second)
	for time.Now().Before(deadline) {
		for _, p := range f.packets() {
			if p["trnm"] == "PING" && p["nonce"] == "abc" {
				return // 통과
			}
		}
		time.Sleep(20 * time.Millisecond)
	}
	t.Fatal("PING echo 가 오지 않았다")
}
```

`errorsAs` 는 `errors.As` 를 짧게 쓴 헬퍼다. `conn_test.go` 맨 아래에 넣는다:

```go
func errorsAs(err error, target any) bool { return errors.As(err, target) }
```

`"errors"` 를 import 에 넣는다.

- [ ] **Step 4: 테스트가 실패하는지 본다**

```bash
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go
go test ./internal/wstransport/
```
Expected: FAIL — `undefined: New`, `undefined: LoginError`

- [ ] **Step 5: 에러 타입을 쓴다**

`internal/wstransport/errors.go`:

```go
// Package wstransport 는 키움 WebSocket 연결을 다룬다.
//
// REST 의 internal/transport 와 형제이고 토큰 발급기(internal/auth)를 공유한다.
// 연결은 경로마다 하나다 — 국내(/api/dostk/websocket)와 미국(/api/us/websocket).
package wstransport

import (
	"fmt"
	"time"
)

// LoginError 는 로그인 패킷에 서버가 return_code != 0 으로 답한 것이다.
type LoginError struct {
	ReturnCode int
	ReturnMsg  string
}

func (e *LoginError) Error() string {
	return fmt.Sprintf("kiwoom: websocket 로그인 실패: %d %s", e.ReturnCode, e.ReturnMsg)
}

// GapError 는 끊겼다가 다시 붙은 구간이다. 그 사이 이벤트는 받지 못했다.
//
// 라이브러리가 대신 메꿔 주지 않는다 — 어떤 REST 호출로 메꿔야 하는지는 종목·타입마다
// 다르고, 우리가 고를 일이 아니다. 사용자가 이 신호를 보고 직접 정한다.
type GapError struct {
	Since time.Time // 끊긴 시각
	Until time.Time // 다시 붙은 시각
}

func (e *GapError) Error() string {
	return fmt.Sprintf("kiwoom: %s ~ %s 동안 실시간이 끊겼다 — 그 구간 이벤트는 받지 못했다",
		e.Since.Format(time.RFC3339), e.Until.Format(time.RFC3339))
}

// SlowConsumerError 는 구독 채널이 가득 차 이벤트를 버렸다는 뜻이다.
//
// 소켓 전체를 막지 않는다 — 한 구독의 느림이 다른 구독을 굶기면 안 된다.
type SlowConsumerError struct {
	Type    string // 실시간 항목 (0B 등)
	Item    string // 종목코드
	Dropped int    // 버린 건수
}

func (e *SlowConsumerError) Error() string {
	return fmt.Sprintf("kiwoom: %s/%s 구독이 느려 %d건을 버렸다 — 채널을 더 빨리 비우거나 버퍼를 늘려라",
		e.Type, e.Item, e.Dropped)
}
```

- [ ] **Step 6: 연결을 쓴다**

`internal/wstransport/conn.go`:

```go
package wstransport

import (
	"context"
	"encoding/json"
	"errors"
	"fmt"
	"strings"
	"sync"
	"time"

	"github.com/coder/websocket"
)

// 도메인. REST 와 달리 포트가 붙는다.
const (
	ProdURL = "wss://api.kiwoom.com:10000"
	MockURL = "wss://mockapi.kiwoom.com:10000"
)

// 경로. 시장마다 연결이 하나씩이다.
const (
	DomesticPath = "/api/dostk/websocket"
	OverseasPath = "/api/us/websocket"
)

// TokenSource 는 접근토큰을 준다. internal/auth.Source 가 만족한다.
type TokenSource interface {
	Token(ctx context.Context) (string, error)
	Invalidate()
}

// envelope 은 모든 WS 메시지의 공통 머리다.
//
// 업무 오류가 HTTP 200 + return_code 로 오는 REST 와 같은 구조다 — trnm 만으로는
// 성공·실패를 가를 수 없다.
type envelope struct {
	Trnm       string          `json:"trnm"`
	ReturnCode int             `json:"return_code"`
	ReturnMsg  string          `json:"return_msg"`
	Data       json.RawMessage `json:"data"`
	raw        []byte          // 원문. 조건검색 응답을 그대로 넘길 때 쓴다
}

// Conn 은 경로 하나에 대한 연결이다. 동시 호출에 안전하다.
type Conn struct {
	url   string
	path  string
	token TokenSource

	mu     sync.Mutex
	ws     *websocket.Conn
	done   chan struct{}      // 수신 루프 종료 신호
	life   context.Context    // 연결의 수명. Connect 의 ctx 와 **다르다**
	stop   context.CancelFunc // Close 가 부른다
	closed bool               // Close 뒤에는 재연결하지 않는다
}

// New 는 연결기를 만든다. 아직 다이얼하지 않는다.
func New(url, path string, token TokenSource) *Conn {
	return &Conn{url: url, path: path, token: token}
}

// Connect 는 연결하고 로그인까지 마친다.
//
// 로그인이 인증 오류로 실패하면 토큰을 버리고 **로그인만** 한 번 다시 시도한다.
// 이것은 요청 재전송이 아니라 연결 수립이므로, 2단계의 "같은 요청을 다시 보내지 않는다"
// 규칙과 어긋나지 않는다 — 주문이 두 번 들어갈 여지가 없다.
func (c *Conn) Connect(ctx context.Context) error {
	// 연결의 수명은 Connect 에 준 ctx 와 다르다.
	//
	// ctx 는 "붙는 데까지" 의 시간 제한이다(예: 5초). 그것을 수신 루프에 그대로 물리면
	// 5초 뒤에 멀쩡한 연결이 죽는다. 수명은 Close 가 끊는다.
	c.mu.Lock()
	if c.life == nil {
		c.life, c.stop = context.WithCancel(context.Background())
	}
	c.mu.Unlock()

	if err := c.connectOnce(ctx); err != nil {
		var le *LoginError
		if !errors.As(err, &le) {
			return err
		}
		c.token.Invalidate()
		return c.connectOnce(ctx)
	}
	return nil
}

func (c *Conn) connectOnce(ctx context.Context) error {
	tok, err := c.token.Token(ctx)
	if err != nil {
		return err
	}

	ws, _, err := websocket.Dial(ctx, c.url+c.path, nil)
	if err != nil {
		return fmt.Errorf("kiwoom: websocket 연결 실패(%s): %w", c.path, err)
	}
	// 실시간 값 메시지는 크다(0D 는 163개 필드). 기본 32KiB 로는 모자랄 수 있다.
	ws.SetReadLimit(1 << 20)

	if err := writeJSON(ctx, ws, map[string]any{"trnm": "LOGIN", "token": tok}); err != nil {
		_ = ws.CloseNow()
		return err
	}

	// 로그인 응답을 **먼저** 받아야 한다. 다른 메시지가 먼저 오면 규약 위반이다.
	env, err := readEnvelope(ctx, ws)
	if err != nil {
		_ = ws.CloseNow()
		return err
	}
	if !strings.EqualFold(env.Trnm, "LOGIN") {
		_ = ws.CloseNow()
		return fmt.Errorf("kiwoom: 로그인 응답 전에 다른 메시지가 왔다: trnm=%q", env.Trnm)
	}
	if env.ReturnCode != 0 {
		_ = ws.CloseNow()
		return &LoginError{ReturnCode: env.ReturnCode, ReturnMsg: env.ReturnMsg}
	}

	c.mu.Lock()
	c.ws = ws
	c.done = make(chan struct{})
	done := c.done
	c.mu.Unlock()

	go c.readLoop(ws, done)
	return nil
}

// lifeCtx 는 연결의 수명 컨텍스트다.
func (c *Conn) lifeCtx() context.Context {
	c.mu.Lock()
	defer c.mu.Unlock()
	if c.life == nil {
		return context.Background()
	}
	return c.life
}

// readLoop 은 메시지를 받아 PING 을 걸러내고 나머지를 dispatch 한다.
func (c *Conn) readLoop(ws *websocket.Conn, done chan struct{}) {
	defer close(done)
	for {
		env, err := readEnvelope(context.Background(), ws)
		if err != nil {
			return
		}
		// PING 은 받은 것을 그대로 되돌려 보내고 위로 올리지 않는다.
		if strings.EqualFold(env.Trnm, "PING") {
			ctx, cancel := context.WithTimeout(context.Background(), 5*time.Second)
			_ = write(ctx, ws, env.raw)
			cancel()
			continue
		}
		c.dispatch(env)
	}
}

// dispatch 는 Task 3·4 에서 채운다. 지금은 버린다.
func (c *Conn) dispatch(env envelope) {}

// Close 는 연결을 닫는다. 두 번 불러도 안전하다.
func (c *Conn) Close() error {
	c.mu.Lock()
	c.closed = true
	stop := c.stop
	ws := c.ws
	c.ws = nil
	c.mu.Unlock()
	if stop != nil {
		stop()
	}
	if ws == nil {
		return nil
	}
	return ws.CloseNow()
}

func readEnvelope(ctx context.Context, ws *websocket.Conn) (envelope, error) {
	_, b, err := ws.Read(ctx)
	if err != nil {
		return envelope{}, err
	}
	// 서버가 따옴표 없는 "PING" 문자열을 보내는 경우가 있다(공식 클라이언트도 이 경우를 본다).
	if s := strings.TrimSpace(string(b)); strings.EqualFold(s, "PING") {
		return envelope{Trnm: "PING", raw: b}, nil
	}
	var env envelope
	if err := json.Unmarshal(b, &env); err != nil {
		return envelope{}, fmt.Errorf("kiwoom: websocket 메시지 파싱 실패: %w", err)
	}
	env.raw = b
	return env, nil
}

func writeJSON(ctx context.Context, ws *websocket.Conn, v any) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}
	return write(ctx, ws, b)
}

func write(ctx context.Context, ws *websocket.Conn, b []byte) error {
	return ws.Write(ctx, websocket.MessageText, b)
}
```

- [ ] **Step 7: 테스트가 통과하는지 본다**

```bash
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go
go test ./internal/wstransport/ -v
```
Expected: 3개 PASS

- [ ] **Step 8: 전체 검사와 커밋**

```bash
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go
go build ./... && go vet ./... && gofmt -l .
git add go.mod go.sum internal/wstransport
git commit -m "feat(ws): 연결·로그인·PING echo" -- go.mod go.sum internal/wstransport
```

---

## Task 3: `internal/wstransport` — 요청/응답 왕복과 구독 라우팅

조건검색은 `trnm` 으로 응답을 짝짓는다(`CNSRLST` 를 보내면 `trnm: "CNSRLST"` 가 온다). 실시간 푸시는 `trnm: "REAL"` 로 오고 `data[].type`·`data[].item` 으로 구독자를 찾는다.

**Files:**
- Create: `internal/wstransport/request.go`
- Create: `internal/wstransport/sub.go`
- Create: `internal/wstransport/sub_test.go`
- Modify: `internal/wstransport/conn.go` (`dispatch` 를 채운다)

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`internal/wstransport/sub_test.go`:

```go
package wstransport

import (
	"context"
	"encoding/json"
	"testing"
	"time"

	"github.com/coder/websocket"
)

func TestSubscribe_REG_패킷을_배열로_보낸다(t *testing.T) {
	f := newFakeServer(t)
	c := New(f.wsURL(), "", &stubToken{token: "TKN"})
	ctx, cancel := context.WithCancel(context.Background())
	defer cancel()
	if err := c.Connect(ctx); err != nil {
		t.Fatalf("Connect: %v", err)
	}
	defer func() { _ = c.Close() }()

	if _, err := c.Subscribe(ctx, "0B", []string{"005930", "000660"}, 4); err != nil {
		t.Fatalf("Subscribe: %v", err)
	}

	reg := waitPacket(t, f, "REG")
	data, _ := reg["data"].([]any)
	if len(data) != 1 {
		t.Fatalf("data 길이 = %d, 기대 1", len(data))
	}
	first, _ := data[0].(map[string]any)
	items, _ := first["item"].([]any)
	types, _ := first["type"].([]any)
	if len(items) != 2 || items[0] != "005930" {
		t.Errorf("item = %v, 기대 [005930 000660] (배열이어야 한다)", first["item"])
	}
	if len(types) != 1 || types[0] != "0B" {
		t.Errorf("type = %v, 기대 [0B] (배열이어야 한다)", first["type"])
	}
}

func TestSubscribe_푸시를_구독자에게_라우팅한다(t *testing.T) {
	f := newFakeServer(t)
	f.onConn = func(t *testing.T, n int, ws *websocket.Conn) {
		go func() {
			time.Sleep(100 * time.Millisecond)
			_ = f.writeJSON(ws, map[string]any{
				"trnm": "REAL",
				"data": []any{map[string]any{
					"type": "0B", "name": "주식체결", "item": "005930",
					"values": map[string]any{"10": "-82000", "20": "093015"},
				}},
			})
		}()
	}

	c := New(f.wsURL(), "", &stubToken{token: "TKN"})
	ctx, cancel := context.WithCancel(context.Background())
	defer cancel()
	if err := c.Connect(ctx); err != nil {
		t.Fatalf("Connect: %v", err)
	}
	defer func() { _ = c.Close() }()

	sub, err := c.Subscribe(ctx, "0B", []string{"005930"}, 4)
	if err != nil {
		t.Fatalf("Subscribe: %v", err)
	}

	select {
	case d := <-sub:
		if d.Err != nil {
			t.Fatalf("Err = %v", d.Err)
		}
		if d.Item != "005930" || d.Type != "0B" {
			t.Errorf("Type/Item = %s/%s, 기대 0B/005930", d.Type, d.Item)
		}
		var vals map[string]string
		if err := json.Unmarshal(d.Values, &vals); err != nil {
			t.Fatalf("values 파싱: %v", err)
		}
		if vals["10"] != "-82000" {
			t.Errorf("values[10] = %q, 기대 -82000", vals["10"])
		}
	case <-time.After(3 * time.Second):
		t.Fatal("푸시가 오지 않았다")
	}
}

func TestSubscribe_ctx_취소가_REMOVE_를_보내고_채널을_닫는다(t *testing.T) {
	f := newFakeServer(t)
	c := New(f.wsURL(), "", &stubToken{token: "TKN"})
	ctx, cancel := context.WithCancel(context.Background())
	defer func() { _ = c.Close() }()
	if err := c.Connect(context.Background()); err != nil {
		t.Fatalf("Connect: %v", err)
	}

	sub, err := c.Subscribe(ctx, "0B", []string{"005930"}, 4)
	if err != nil {
		t.Fatalf("Subscribe: %v", err)
	}
	waitPacket(t, f, "REG")

	cancel()
	waitPacket(t, f, "REMOVE")

	select {
	case _, ok := <-sub:
		if ok {
			// 남은 이벤트를 다 비우면 닫힌다.
			for range sub {
			}
		}
	case <-time.After(3 * time.Second):
		t.Fatal("채널이 닫히지 않았다")
	}
}

func TestRequest_trnm_으로_응답을_짝짓는다(t *testing.T) {
	f := newFakeServer(t)
	f.onConn = func(t *testing.T, n int, ws *websocket.Conn) {
		go func() {
			time.Sleep(100 * time.Millisecond)
			_ = f.writeJSON(ws, map[string]any{
				"trnm": "CNSRLST", "return_code": 0,
				"data": []any{map[string]any{"seq": "0", "name": "내조건"}},
			})
		}()
	}

	c := New(f.wsURL(), "", &stubToken{token: "TKN"})
	ctx, cancel := context.WithTimeout(context.Background(), 5*time.Second)
	defer cancel()
	if err := c.Connect(ctx); err != nil {
		t.Fatalf("Connect: %v", err)
	}
	defer func() { _ = c.Close() }()

	var out struct {
		Data []struct {
			Seq  string `json:"seq"`
			Name string `json:"name"`
		} `json:"data"`
	}
	if err := c.Request(ctx, "CNSRLST", map[string]any{"trnm": "CNSRLST"}, &out); err != nil {
		t.Fatalf("Request: %v", err)
	}
	if len(out.Data) != 1 || out.Data[0].Name != "내조건" {
		t.Errorf("응답 = %+v, 기대 내조건 1건", out.Data)
	}
}

// waitPacket 은 서버가 그 trnm 패킷을 받을 때까지 기다린다.
func waitPacket(t *testing.T, f *fakeServer, trnm string) map[string]any {
	t.Helper()
	deadline := time.Now().Add(3 * time.Second)
	for time.Now().Before(deadline) {
		for _, p := range f.packets() {
			if p["trnm"] == trnm {
				return p
			}
		}
		time.Sleep(20 * time.Millisecond)
	}
	t.Fatalf("%s 패킷이 오지 않았다", trnm)
	return nil
}
```

- [ ] **Step 2: 실패를 확인한다**

```bash
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go
go test ./internal/wstransport/
```
Expected: FAIL — `c.Subscribe undefined`, `c.Request undefined`

- [ ] **Step 3: 구독을 쓴다**

`internal/wstransport/sub.go`:

```go
package wstransport

import (
	"context"
	"encoding/json"
	"time"
)

// Delivery 는 라우팅된 실시간 한 건이다.
//
// Values 를 맵으로 풀지 않고 원문으로 넘기는 이유: 호출자가 같은 원문을 두 번 읽어
// (1) 타입 구조체와 (2) Raw 맵을 함께 채운다. 여기서 맵으로 풀면 타입 쪽이 다시
// 직렬화를 거쳐야 한다.
type Delivery struct {
	Type   string          // 실시간 항목 (0B 등)
	Name   string          // 실시간 항목명
	Item   string          // 종목코드
	Values json.RawMessage // FID → 값
	Time   time.Time       // 수신 시각
	Err    error           // *GapError · *SlowConsumerError. 이때 나머지는 비어 있다
}

// realPayload 는 REAL 메시지의 data 원소다.
type realPayload struct {
	Type   string          `json:"type"`
	Name   string          `json:"name"`
	Item   string          `json:"item"`
	Values json.RawMessage `json:"values"`
}

type subKey struct{ typ, item string }

type sub struct {
	key     subKey
	ch      chan Delivery
	dropped int
}

// Subscribe 는 실시간 등록(REG)을 보내고 채널을 준다.
//
// ctx 가 취소되면 해지(REMOVE)를 보내고 채널을 닫는다. buf 는 채널 버퍼 크기다 —
// 가득 차면 이벤트를 버리고 *SlowConsumerError 를 흘린다. 소켓 전체를 막지 않는다.
func (c *Conn) Subscribe(ctx context.Context, typ string, items []string, buf int) (<-chan Delivery, error) {
	if buf < 1 {
		buf = 1
	}
	ch := make(chan Delivery, buf)

	c.subMu.Lock()
	if c.subs == nil {
		c.subs = map[subKey][]*sub{}
	}
	made := make([]*sub, 0, len(items))
	for _, it := range items {
		s := &sub{key: subKey{typ, it}, ch: ch}
		c.subs[s.key] = append(c.subs[s.key], s)
		made = append(made, s)
	}
	c.regs = append(c.regs, registration{typ: typ, items: items})
	c.subMu.Unlock()

	if err := c.sendReg(ctx, "REG", typ, items); err != nil {
		c.drop(made)
		close(ch)
		return nil, err
	}

	go func() {
		<-ctx.Done()
		// 해지는 best-effort 다. 연결이 이미 죽었으면 보낼 곳이 없다.
		rmCtx, cancel := context.WithTimeout(context.Background(), 5*time.Second)
		_ = c.sendReg(rmCtx, "REMOVE", typ, items)
		cancel()
		c.drop(made)
		close(ch)
	}()

	return ch, nil
}

// registration 은 재연결 때 다시 보낼 등록이다(Task 4).
type registration struct {
	typ   string
	items []string
}

func (c *Conn) sendReg(ctx context.Context, trnm, typ string, items []string) error {
	c.mu.Lock()
	ws := c.ws
	c.mu.Unlock()
	if ws == nil {
		return errNotConnected
	}
	return writeJSON(ctx, ws, map[string]any{
		"trnm":    trnm,
		"grp_no":  "1",
		"refresh": "1",
		// item·type 은 **배열**이다. 스펙의 depth-1 String 선언과 다르다(SOURCE.md 참고).
		"data": []any{map[string]any{"item": items, "type": []string{typ}}},
	})
}

func (c *Conn) drop(ss []*sub) {
	c.subMu.Lock()
	defer c.subMu.Unlock()
	for _, s := range ss {
		list := c.subs[s.key]
		for i, x := range list {
			if x == s {
				c.subs[s.key] = append(list[:i:i], list[i+1:]...)
				break
			}
		}
		if len(c.subs[s.key]) == 0 {
			delete(c.subs, s.key)
		}
	}
}

// routeReal 은 REAL 메시지를 구독자에게 나눠 준다.
func (c *Conn) routeReal(env envelope) {
	var payloads []realPayload
	if err := json.Unmarshal(env.Data, &payloads); err != nil {
		return
	}
	now := time.Now()
	for _, p := range payloads {
		c.subMu.Lock()
		list := append([]*sub(nil), c.subs[subKey{p.Type, p.Item}]...)
		c.subMu.Unlock()
		for _, s := range list {
			c.deliver(s, Delivery{
				Type: p.Type, Name: p.Name, Item: p.Item,
				Values: p.Values, Time: now,
			})
		}
	}
}

// deliver 는 막히지 않고 보낸다. 가득 차면 버리고 한 번만 알린다.
func (c *Conn) deliver(s *sub, d Delivery) {
	select {
	case s.ch <- d:
	default:
		c.subMu.Lock()
		s.dropped++
		n := s.dropped
		c.subMu.Unlock()
		// 알림도 막히면 그냥 버린다 — 느리다고 알리다가 같이 막히면 뜻이 없다.
		select {
		case s.ch <- Delivery{Err: &SlowConsumerError{Type: s.key.typ, Item: s.key.item, Dropped: n}}:
		default:
		}
	}
}
```

- [ ] **Step 4: 요청/응답을 쓴다**

`internal/wstransport/request.go`:

```go
package wstransport

import (
	"context"
	"encoding/json"
	"errors"
	"fmt"
	"strings"
)

var errNotConnected = errors.New("kiwoom: websocket 이 연결되어 있지 않다")

// Request 는 패킷을 보내고 같은 trnm 의 응답을 기다린다.
//
// 짝짓기 열쇠가 trnm 뿐이다 — 키움 WS 에는 요청 식별자가 없다. 그래서 같은 trnm 의
// 요청을 동시에 두 건 보내면 응답이 뒤바뀔 수 있다. 조건검색은 그럴 일이 없지만,
// 그렇게 쓰지 못하도록 trnm 당 한 건만 대기시킨다.
//
// **재전송하지 않는다.** REST 와 같은 규칙이다(설계 §5).
func (c *Conn) Request(ctx context.Context, trnm string, body any, out any) error {
	key := strings.ToUpper(trnm)

	waiter := make(chan envelope, 1)
	c.reqMu.Lock()
	if c.reqs == nil {
		c.reqs = map[string]chan envelope{}
	}
	if _, busy := c.reqs[key]; busy {
		c.reqMu.Unlock()
		return fmt.Errorf("kiwoom: %s 요청이 이미 대기 중이다 — 같은 trnm 을 동시에 보내지 마라", key)
	}
	c.reqs[key] = waiter
	c.reqMu.Unlock()

	defer func() {
		c.reqMu.Lock()
		delete(c.reqs, key)
		c.reqMu.Unlock()
	}()

	c.mu.Lock()
	ws := c.ws
	c.mu.Unlock()
	if ws == nil {
		return errNotConnected
	}
	if err := writeJSON(ctx, ws, body); err != nil {
		return err
	}

	select {
	case env := <-waiter:
		if env.ReturnCode != 0 {
			return &APIError{Trnm: env.Trnm, ReturnCode: env.ReturnCode, ReturnMsg: env.ReturnMsg}
		}
		if out != nil {
			if err := json.Unmarshal(env.raw, out); err != nil {
				return fmt.Errorf("kiwoom: %s 응답 파싱 실패: %w", key, err)
			}
		}
		return nil
	case <-ctx.Done():
		return ctx.Err()
	}
}

// APIError 는 업무 오류다. REST 의 transport.APIError 와 같은 자리다 —
// 성공·실패를 return_code 로 가른다.
type APIError struct {
	Trnm       string
	ReturnCode int
	ReturnMsg  string
}

func (e *APIError) Error() string {
	return fmt.Sprintf("kiwoom: %s 실패: %d %s", e.Trnm, e.ReturnCode, e.ReturnMsg)
}

// routeResponse 는 대기 중인 요청에 응답을 건넨다. 짝이 없으면 false.
func (c *Conn) routeResponse(env envelope) bool {
	c.reqMu.Lock()
	w, ok := c.reqs[strings.ToUpper(env.Trnm)]
	c.reqMu.Unlock()
	if !ok {
		return false
	}
	select {
	case w <- env:
	default:
	}
	return true
}
```

- [ ] **Step 5: `conn.go` 에 필드를 더하고 `dispatch` 를 채운다**

`Conn` 구조체를 이렇게 바꾼다:

```go
type Conn struct {
	url   string
	path  string
	token TokenSource

	mu   sync.Mutex
	ws   *websocket.Conn
	done chan struct{}

	subMu sync.Mutex
	subs  map[subKey][]*sub
	regs  []registration // 재연결 때 다시 보낼 등록(Task 4)

	reqMu sync.Mutex
	reqs  map[string]chan envelope
}
```

`dispatch` 를 이렇게 바꾼다:

```go
// dispatch 는 PING 이 아닌 메시지를 요청 대기자 또는 구독자에게 보낸다.
func (c *Conn) dispatch(env envelope) {
	if strings.EqualFold(env.Trnm, "REAL") {
		c.routeReal(env)
		return
	}
	// 요청 응답이 아니면 버린다. REG/REMOVE 의 확인 응답이 여기로 온다 —
	// 보낼 곳이 없으므로 버리는 것이 맞다.
	_ = c.routeResponse(env)
}
```

- [ ] **Step 6: 통과를 확인한다**

```bash
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go
go test ./internal/wstransport/ -v -race
```
Expected: 7개 PASS (Task 2 의 3개 + 이번 4개). `-race` 가 깨끗해야 한다.

- [ ] **Step 7: 커밋**

```bash
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go
go build ./... && go vet ./... && gofmt -l .
git add internal/wstransport
git commit -m "feat(ws): 요청/응답 짝짓기와 구독 라우팅" -- internal/wstransport
```

---

## Task 4: `internal/wstransport` — 재연결·재구독·구멍 신호

끊기면 다시 붙고 등록을 다시 보낸다. **그 사이 이벤트는 못 받으므로 `*GapError` 를 흘린다.**

**Files:**
- Create: `internal/wstransport/reconnect.go`
- Create: `internal/wstransport/reconnect_test.go`
- Modify: `internal/wstransport/conn.go` (`readLoop` 이 끊김을 알리게)

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`internal/wstransport/reconnect_test.go`:

```go
package wstransport

import (
	"context"
	"errors"
	"testing"
	"time"

	"github.com/coder/websocket"
)

func TestReconnect_끊기면_다시_붙고_재구독한다(t *testing.T) {
	f := newFakeServer(t)
	f.onConn = func(t *testing.T, n int, ws *websocket.Conn) {
		if n == 1 {
			// 첫 연결은 REG 를 받은 뒤 끊는다.
			go func() {
				time.Sleep(200 * time.Millisecond)
				_ = ws.CloseNow()
			}()
		}
	}

	c := New(f.wsURL(), "", &stubToken{token: "TKN"})
	c.retryDelay = 20 * time.Millisecond // 테스트를 빠르게
	ctx, cancel := context.WithCancel(context.Background())
	defer cancel()
	if err := c.Connect(ctx); err != nil {
		t.Fatalf("Connect: %v", err)
	}
	defer func() { _ = c.Close() }()

	if _, err := c.Subscribe(ctx, "0B", []string{"005930"}, 8); err != nil {
		t.Fatalf("Subscribe: %v", err)
	}
	waitPacket(t, f, "REG")

	// 두 번째 연결이 생기고, 거기에도 REG 가 다시 가야 한다.
	deadline := time.Now().Add(5 * time.Second)
	for time.Now().Before(deadline) {
		regs := 0
		for _, p := range f.packets() {
			if p["trnm"] == "REG" {
				regs++
			}
		}
		if f.connCount() >= 2 && regs >= 2 {
			return // 통과
		}
		time.Sleep(20 * time.Millisecond)
	}
	t.Fatalf("재연결/재구독이 없었다 (연결 %d회)", f.connCount())
}

func TestReconnect_구멍을_채널로_알린다(t *testing.T) {
	f := newFakeServer(t)
	f.onConn = func(t *testing.T, n int, ws *websocket.Conn) {
		if n == 1 {
			go func() {
				time.Sleep(200 * time.Millisecond)
				_ = ws.CloseNow()
			}()
		}
	}

	c := New(f.wsURL(), "", &stubToken{token: "TKN"})
	c.retryDelay = 20 * time.Millisecond
	ctx, cancel := context.WithCancel(context.Background())
	defer cancel()
	if err := c.Connect(ctx); err != nil {
		t.Fatalf("Connect: %v", err)
	}
	defer func() { _ = c.Close() }()

	sub, err := c.Subscribe(ctx, "0B", []string{"005930"}, 8)
	if err != nil {
		t.Fatalf("Subscribe: %v", err)
	}

	timeout := time.After(5 * time.Second)
	for {
		select {
		case d := <-sub:
			var ge *GapError
			if d.Err != nil && errors.As(d.Err, &ge) {
				if ge.Since.IsZero() || ge.Until.IsZero() {
					t.Errorf("GapError 의 시각이 비어 있다: %+v", ge)
				}
				return // 통과
			}
		case <-timeout:
			t.Fatal("GapError 가 오지 않았다")
		}
	}
}
```

- [ ] **Step 2: 실패를 확인한다**

```bash
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go
go test ./internal/wstransport/ -run Reconnect
```
Expected: FAIL — `c.retryDelay undefined`

- [ ] **Step 3: 재연결을 쓴다**

`internal/wstransport/reconnect.go`:

```go
package wstransport

import (
	"context"
	"time"
)

// defaultRetryDelay 는 재연결 간격이다.
//
// 지수 백오프를 쓰지 않는다 — 실시간은 늦게 붙을수록 구멍이 커지고, 키움이 WS 재연결
// 한도를 문서로 밝히지 않았다. 고정 간격이 예측 가능하고, 구멍은 GapError 로 드러난다.
const defaultRetryDelay = 2 * time.Second

// reconnectLoop 은 연결이 끊길 때마다 다시 붙고 등록을 다시 보낸다.
//
// 재연결은 **받기만 하는 쪽**이라 2단계의 "재시도하지 않는다"와 어긋나지 않는다 —
// 실시간 등록을 다시 보내는 것은 주문을 두 번 넣지 않는다. 조건검색 요청(Request)은
// 다시 보내지 않는다.
func (c *Conn) reconnectLoop(ctx context.Context, since time.Time) {
	delay := c.retryDelay
	if delay <= 0 {
		delay = defaultRetryDelay
	}

	for {
		select {
		case <-ctx.Done():
			return
		case <-time.After(delay):
		}

		if err := c.connectOnce(ctx); err != nil {
			continue
		}

		// 붙었으면 등록을 다시 보내고, 구멍을 알린다.
		c.subMu.Lock()
		regs := append([]registration(nil), c.regs...)
		c.subMu.Unlock()
		for _, r := range regs {
			sendCtx, cancel := context.WithTimeout(ctx, 5*time.Second)
			_ = c.sendReg(sendCtx, "REG", r.typ, r.items)
			cancel()
		}
		c.notifyGap(since, time.Now())
		return
	}
}

// notifyGap 은 모든 구독자에게 끊긴 구간을 알린다.
//
// 조용히 넘기지 않는 이유: 사용자가 구멍을 알아야 REST 로 메꿀지 정할 수 있다.
// 이것을 감추면 "왜 어떤 날은 데이터가 비지?" 를 아무도 설명하지 못하게 된다.
func (c *Conn) notifyGap(since, until time.Time) {
	c.subMu.Lock()
	var all []*sub
	for _, list := range c.subs {
		all = append(all, list...)
	}
	c.subMu.Unlock()

	gap := &GapError{Since: since, Until: until}
	seen := map[chan Delivery]bool{}
	for _, s := range all {
		// 한 채널에 여러 종목이 실려 있으면 한 번만 알린다.
		if seen[s.ch] {
			continue
		}
		seen[s.ch] = true
		select {
		case s.ch <- Delivery{Err: gap, Time: until}:
		default:
		}
	}
}
```

- [ ] **Step 4: `conn.go` 를 고친다**

`Conn` 에 필드 하나를 더한다(`closed`·`life`·`stop` 은 Task 2 에서 이미 넣었다):

```go
	retryDelay time.Duration // 0 이면 defaultRetryDelay. 테스트가 줄인다
```

`readLoop` 을 이렇게 바꾼다. **`Connect` 에 준 ctx 가 아니라 연결의 수명 ctx 를 쓴다** —
"붙는 데까지 5초" 가 멀쩡한 연결을 5초 뒤에 죽이면 안 된다:

```go
// readLoop 은 메시지를 받아 PING 을 걸러내고 나머지를 dispatch 한다.
//
// 읽기가 실패하면 연결이 죽은 것이다 — 재연결 루프에 넘긴다.
func (c *Conn) readLoop(ws *websocket.Conn, done chan struct{}) {
	defer close(done)
	ctx := c.lifeCtx()
	for {
		env, err := readEnvelope(ctx, ws)
		if err != nil {
			c.mu.Lock()
			closed := c.closed
			c.mu.Unlock()
			if !closed && ctx.Err() == nil {
				go c.reconnectLoop(ctx, time.Now())
			}
			return
		}
		if strings.EqualFold(env.Trnm, "PING") {
			wctx, cancel := context.WithTimeout(ctx, 5*time.Second)
			_ = write(wctx, ws, env.raw)
			cancel()
			continue
		}
		c.dispatch(env)
	}
}
```

`readLoop` 의 시그니처가 그대로이므로 `connectOnce` 의 호출부는 고치지 않는다.

- [ ] **Step 5: 통과를 확인한다**

```bash
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go
go test ./internal/wstransport/ -v -race -count=1
```
Expected: 9개 PASS. `-race` 깨끗.

`-count=1` 로 캐시를 끈다 — 타이밍이 있는 테스트라 캐시된 PASS 를 믿으면 안 된다.

- [ ] **Step 6: 커밋**

```bash
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go
go build ./... && go vet ./... && gofmt -l .
git add internal/wstransport
git commit -m "feat(ws): 재연결·재구독과 구멍 신호" -- internal/wstransport
```

---

## Task 5: 루트에 WS 를 노출한다

생성물과 사용자가 `internal/wstransport` 의 타입을 **이름으로** 쓸 수 있어야 한다. 외부 모듈은 `internal/` 을 import 하지 못하므로 루트에 별칭을 둔다(1단계에서 `transport.Meta` 로 같은 문제를 겪었다).

**Files:**
- Create: `stream.go`
- Create: `stream_test.go`
- Modify: `config.go`
- Modify: `client.go`

- [ ] **Step 1: `config.go` 에 WS 설정을 더한다**

`const` 블록 아래에 더한다:

```go
// WebSocket 도메인. REST 와 달리 포트가 붙는다.
const (
	ProdWSURL = wstransport.ProdURL
	MockWSURL = wstransport.MockURL
)
```

`options` 에 필드 하나를 더한다:

```go
type options struct {
	baseURL    string
	wsBaseURL  string
	timeout    time.Duration
	httpClient *http.Client
}
```

`WithMock` 을 고치고 옵션 하나를 더한다:

```go
// WithMock 은 모의투자 도메인을 쓴다(REST·WebSocket 둘 다).
//
// WithBaseURL 과 함께 주면 **나중에 준 것이 이긴다**(functional option 의 일반 규칙).
func WithMock() Option {
	return func(o *options) { o.baseURL, o.wsBaseURL = MockBaseURL, MockWSURL }
}

// WithWSBaseURL 은 WebSocket 베이스 URL 을 직접 지정한다(테스트·프록시용).
//
// WithBaseURL 은 REST 만 바꾼다 — 두 주소는 호스트도 포트도 다르므로 하나로 묶으면
// REST 프록시를 붙이려던 사용자의 WebSocket 이 조용히 엉뚱한 곳으로 간다.
func WithWSBaseURL(u string) Option { return func(o *options) { o.wsBaseURL = u } }
```

`"github.com/kenshin579/kiwoom-go/internal/wstransport"` 를 import 에 넣는다.

- [ ] **Step 2: `client.go` 에서 WS 연결 둘을 만든다**

`NewClient` 의 기본값과 반환부를 고친다:

```go
	o := options{baseURL: ProdBaseURL, wsBaseURL: ProdWSURL, timeout: defaultTimeout}
```

```go
	if o.wsBaseURL == "" {
		o.wsBaseURL = ProdWSURL
	}
```
(`o.baseURL == ""` 가드 바로 아래에 넣는다. 같은 이유다 — 빈 값이 상대 경로가 되면 안 된다.)

```go
	tok := auth.New(o.baseURL, appKey, secretKey, hc)
	tr := transport.New(o.baseURL, hc, tok)
	dom := wstransport.New(o.wsBaseURL, wstransport.DomesticPath, tok)
	ovs := wstransport.New(o.wsBaseURL, wstransport.OverseasPath, tok)
	return &Client{
		baseURL: o.baseURL,
		Clients: newClients(tr, dom, ovs),
	}, nil
```

`newClients` 의 새 시그니처는 Task 9 에서 생성물이 만든다. **Task 9 전까지 루트는 컴파일되지 않는다** — 정상이다. Step 4 에서 이것을 다룬다.

- [ ] **Step 3: `stream.go` 를 쓴다**

```go
package kiwoom

import "github.com/kenshin579/kiwoom-go/internal/wstransport"

// Event 는 실시간 한 건이다.
//
// Err 이 nil 이 아니면 Value 는 영값이다 — 구멍(*GapError)이나 느린 소비자
// (*SlowConsumerError)를 알리는 봉투이기 때문이다.
type Event[T any] struct {
	Symbol string // 종목코드
	Name   string // 실시간 항목명
	Time   time.Time
	Value  T
	Raw    map[string]string // 받은 FID 전부. 표에 없는 것도 여기 남는다
	Err    error
}

// 아래는 internal/wstransport 의 타입을 외부에서 이름으로 쓰게 하는 별칭이다.
//
// 외부 모듈은 internal/ 을 import 하지 못한다. 1단계에서 연속조회 옵션이 이 문제로
// 막혔던 적이 있어(call.go 참고), WebSocket 쪽도 같은 방식으로 먼저 열어 둔다.
type (
	// GapError 는 끊겼다가 다시 붙은 구간이다. 그 사이 이벤트는 받지 못했다.
	GapError = wstransport.GapError
	// SlowConsumerError 는 구독 채널이 가득 차 이벤트를 버렸다는 뜻이다.
	SlowConsumerError = wstransport.SlowConsumerError
	// WSLoginError 는 WebSocket 로그인이 거부된 것이다.
	WSLoginError = wstransport.LoginError
	// WSAPIError 는 WebSocket 업무 오류(return_code != 0)다.
	WSAPIError = wstransport.APIError
)
```

`"time"` 을 import 에 넣는다.

- [ ] **Step 4: 컴파일이 아직 깨지는 것을 확인한다**

```bash
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go
go build ./... 2>&1 | head -5
```
Expected: `newClients` 인자 개수 불일치 에러. **이것이 맞다** — 생성물이 Task 9 에서 따라온다.

`internal/` 만 따로 확인한다:

```bash
go build ./internal/... && go test ./internal/... -count=1
```
Expected: 통과

- [ ] **Step 5: 커밋**

루트가 아직 안 붙으므로 `stream_test.go` 는 Task 9 에서 쓴다. 지금은 코드만 커밋한다.

```bash
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go
gofmt -l config.go client.go stream.go
git add config.go client.go stream.go
git commit -m "feat: WebSocket URL 옵션과 Event·에러 타입 노출" -- config.go client.go stream.go
```

**보고할 것:** 루트가 의도적으로 컴파일되지 않는 상태로 커밋됐다는 사실. Task 9 가 닫는다.

---

## Task 6: FID 표 400개 초안

**이 저장소에서 손으로 짓는 두 번째 이름표다.** 첫 번째는 `groups.go` 의 카테고리 25개였다.

**Files:**
- Create: `tools/gen/fids.go`
- Create: `tools/gen/fids_test.go`

- [ ] **Step 1: 스펙에서 FID 목록을 뽑는다**

```bash
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go/tools
python3 - <<'PY' > /tmp/fids.txt
import json, collections
s = json.load(open('spec/kiwoom_api_spec.json'))
seen = collections.OrderedDict()
for k, v in sorted(s['apis'].items()):
    menu = (v.get('meta') or {}).get('메뉴 위치', '')
    if '실시간시세' not in menu and '조건검색' not in menu:
        continue
    for f in (v.get('response') or {}).get('body') or []:
        if f.get('depth') == 2 and str(f['element']).isdigit():
            seen.setdefault(f['element'], set()).add(f['한글명'].strip())
for fid, names in sorted(seen.items(), key=lambda x: int(x[0])):
    ns = sorted(n for n in names if n)
    print(f"{fid}\t{ns[0]}\t{'|'.join(ns[1:])}")
print(f"# 합계 {len(seen)}", flush=True)
PY
tail -1 /tmp/fids.txt
```
Expected: `# 합계 400`

- [ ] **Step 2: `fids.go` 를 쓴다**

`/tmp/fids.txt` 의 400줄을 표로 옮긴다. 형식:

```go
package gen

// Fid 는 실시간 값 필드 하나다.
type Fid struct {
	FID    string // 스펙의 element. 숫자다
	Name   string // Go 필드 이름
	Korean string // 스펙의 한글명. 사람이 표를 훑을 수 있게 남긴다
}

// Fids 는 FID → Go 필드 이름 표다.
//
// **여기는 지어낸 이름이다.** groups.go 와 같은 이유로 손으로 적는다 — 스펙이 주는 것은
// FID 숫자와 한글명뿐이고, 영문명은 어디에도 없다. GoName 에 맡기면 "N10"·"N9001" 이 되어
// 공개 라이브러리 표면으로 쓸 수 없다.
//
// 한글명을 모든 줄에 남기는 이유: 틀린 이름은 표를 훑으면 사람 눈에 보인다.
// 이름을 고칠 때는 **생성물을 고치지 말고** 이 표를 고치고 다시 생성하라.
//
// 한글명이 갈리는 FID 가 66개 있다. 전부 표기 차이다(`매도1호가` vs `매도호가1`,
// `시간` vs `체결시간`). 의미가 실제로 충돌하는 FID 는 없어서 하나로 접었고,
// 버린 표기는 그 줄 주석에 남겼다.
var Fids = []Fid{
	{"9", "MarketCapHundredMillionWon", "시가총액(억)"},
	{"10", "CurrentPrice", "현재가"},
	{"11", "PrevDayDiff", "전일대비"},
	{"12", "ChangeRate", "등락율"},
	{"13", "CumulativeVolume", "누적거래량"},
	// … 400줄
	{"20", "TradeTime", "체결시간"}, // 표기 차이: 시간
	// …
}

// fidByID 는 조회용 인덱스다. 슬라이스로 두는 이유는 생성 순서를 고정하기 위해서다.
var fidByID = func() map[string]Fid {
	m := make(map[string]Fid, len(Fids))
	for _, f := range Fids {
		m[f.FID] = f
	}
	return m
}()

// LookupFid 는 FID 로 Go 이름을 찾는다.
func LookupFid(fid string) (Fid, bool) {
	f, ok := fidByID[fid]
	return f, ok
}
```

**이름 규칙:**
- 한글명을 영어로 옮긴다. 축약하지 않는다 — `현재가` → `CurrentPrice`(`CurPrc` 아님).
- 번호가 붙은 것은 뒤에 숫자를 붙인다 — `매도호가1` → `AskPrice1`.
- 단위 괄호는 이름에 녹인다 — `시가총액(억)` → `MarketCapHundredMillionWon`.
  **자릿수를 확인하라** — 억은 10⁸, Billion 은 10⁹ 다. `MarketCapBillion` 이라고 쓰면
  읽는 사람이 값을 10배 틀리게 보고, 그걸 알려 줄 신호가 아무 데도 없다.
- **한글명만 보지 말고 스펙의 `description` 열을 함께 보라.** 단위(`단위: 억원`·`단위: 백만원`)와
  코드 도메인(`1:매도, 2:매수`)이 거기 있다. 한글명에 없는 것이 많고, 애매한 이름은 대개
  거기서 갈린다.
- Go 식별자로 시작할 수 없는 이름을 만들지 않는다.
- **같은 이름을 두 FID 에 주지 않는다.** Step 3 의 테스트가 잡는다.

- [ ] **Step 3: 완전성 테스트를 쓴다**

`tools/gen/fids_test.go`:

```go
package gen_test

import (
	"path/filepath"
	"regexp"
	"sort"
	"strings"
	"testing"

	"github.com/kenshin579/kiwoom-go/tools/gen"
)

const fidSpecDir = "../spec"

// TestFids_스펙의_모든_FID_가_표에_있다 는 이름표가 빠진 FID 를 잡는다.
//
// groups.go 의 카테고리 완전성 테스트와 같은 성격이다 — 빠진 것이 조용히 넘어가면
// 생성물에서 필드 하나가 통째로 사라진다.
func TestFids_스펙의_모든_FID_가_표에_있다(t *testing.T) {
	spec, err := gen.LoadSpec(filepath.Join(fidSpecDir, "kiwoom_api_spec.json"))
	if err != nil {
		t.Fatalf("스펙 읽기: %v", err)
	}

	want := map[string]string{} // fid → 한글명
	for _, api := range spec.APIs {
		menu := api.Meta["메뉴 위치"]
		if !strings.Contains(menu, "실시간시세") && !strings.Contains(menu, "조건검색") {
			continue
		}
		for _, f := range api.Response.Body {
			if f.Depth == 2 && isDigits(f.Element) {
				want[f.Element] = f.Korean
			}
		}
	}

	var missing []string
	for fid := range want {
		if _, ok := gen.LookupFid(fid); !ok {
			missing = append(missing, fid+"("+want[fid]+")")
		}
	}
	sort.Strings(missing)
	if len(missing) > 0 {
		t.Errorf("표에 없는 FID %d개 — tools/gen/fids.go 에 넣어라:\n  %v", len(missing), missing)
	}
}

// TestFids_이름이_겹치지_않는다 는 같은 Go 이름을 두 FID 에 준 것을 잡는다.
//
// 겹치면 같은 구조체에 같은 필드가 두 번 선언돼 컴파일이 깨지지만, 어느 FID 때문인지
// 생성물 쪽 에러로는 알기 어렵다. 표에서 잡는 편이 훨씬 빠르다.
func TestFids_이름이_겹치지_않는다(t *testing.T) {
	byName := map[string][]string{}
	for _, f := range gen.Fids {
		byName[f.Name] = append(byName[f.Name], f.FID)
	}
	for name, fids := range byName {
		if len(fids) > 1 {
			sort.Strings(fids)
			t.Errorf("Go 이름 %q 를 FID %v 가 함께 쓴다", name, fids)
		}
	}
}

var goIdent = regexp.MustCompile(`^[A-Z][A-Za-z0-9]*$`)

// TestFids_이름이_Go_식별자다 는 소문자 시작·기호·빈 이름을 잡는다.
func TestFids_이름이_Go_식별자다(t *testing.T) {
	for _, f := range gen.Fids {
		if !goIdent.MatchString(f.Name) {
			t.Errorf("FID %s 의 이름 %q 는 내보낼 수 있는 Go 식별자가 아니다", f.FID, f.Name)
		}
		if f.Korean == "" {
			t.Errorf("FID %s 에 한글명이 없다 — 사람이 표를 훑을 수 없다", f.FID)
		}
	}
}

func isDigits(s string) bool {
	if s == "" {
		return false
	}
	for _, r := range s {
		if r < '0' || r > '9' {
			return false
		}
	}
	return true
}
```

- [ ] **Step 4: 테스트를 돌린다**

```bash
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go/tools
go test ./gen/ -run Fids -v
```
Expected: 3개 PASS. 실패하면 **표를 고친다**(테스트를 고치지 않는다).

- [ ] **Step 5: 커밋**

```bash
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go
gofmt -l tools/
git add tools/gen/fids.go tools/gen/fids_test.go
git commit -m "feat(gen): FID 이름표 400개" -- tools/gen/fids.go tools/gen/fids_test.go
```

---

## Task 7: FID 표를 사람 눈으로 검토한다

설계 §6 이 "검토를 구현과 분리한다"고 정했다. 400개는 훑어야 보인다. **이 작업은 코드를 바꾸지 않고 표만 고친다.**

**Files:**
- Modify: `tools/gen/fids.go` (이름만)

- [ ] **Step 1: 표를 한글명과 나란히 놓고 본다**

```bash
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go
grep -oP '\{"\d+",\s*"\K[^"]+(?=",\s*")' tools/gen/fids.go | head -20
awk '/^\t\{"/ {print}' tools/gen/fids.go | column -t -s'"' | head -40
```

- [ ] **Step 2: 다음을 하나씩 확인한다**

각 항목마다 문제를 찾으면 `tools/gen/fids.go` 를 고친다.

1. **뜻이 뒤집힌 것** — `매도`(ask/sell)와 `매수`(bid/buy)를 바꿔 쓰지 않았나. 이게 제일 위험하다. 호가·체결에서 한 글자 차이로 반대가 된다.
2. **단위가 이름에 없어 오해를 부르는 것** — `(억)`·`(비율)`·`(계약,주)` 가 붙은 한글명인데 이름이 맨숭맨숭한 경우.
3. **번호 매김이 뒤집힌 것** — `매도10차선잔량` 류에서 번호가 맞게 붙었나.
4. **부호/기호 필드** — `전일대비기호` 는 값이 부호 코드다. `PrevDayDiffSign` 처럼 기호임이 드러나야 한다.
5. **시간 필드** — `체결시간`·`호가시간`·`시가시간` 이 전부 `Time` 하나로 뭉개지지 않았나.
6. **약어를 만들지 않았나** — `CurPrc` 같은 것. 규칙은 축약하지 않는 것이다.

- [ ] **Step 3: 고친 뒤 테스트를 다시 돌린다**

```bash
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go/tools
go test ./gen/ -run Fids -count=1
```
Expected: PASS

- [ ] **Step 4: 커밋**

고친 것이 없으면 이 Task 는 커밋 없이 끝난다. 그 경우 **무엇을 확인했고 왜 고칠 것이 없었는지** 보고한다.

```bash
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go
git add tools/gen/fids.go
git commit -m "fix(gen): FID 이름 검토 반영" -- tools/gen/fids.go
```

---

## Task 8: 생성기에 `Kind` 와 두 템플릿을 더한다

**Files:**
- Modify: `tools/gen/groups.go`
- Create: `tools/gen/render_realtime.go`
- Create: `tools/gen/render_realtime_test.go`
- Create: `tools/gen/render_condition.go`
- Create: `tools/gen/render_condition_test.go`
- Modify: `tools/gen/render_client.go`

- [ ] **Step 1: `Group` 에 `Kind` 를 더한다**

`tools/gen/groups.go` 의 `Group` 에 필드를 더한다:

```go
// Kind 는 이 그룹을 어떤 템플릿으로 만드는지다.
//
// REST 와 WebSocket 은 전송 계층이 다르다 — 하위 클라이언트가 받는 것도 다르다.
type Kind string

const (
	KindREST      Kind = "rest"      // HTTP POST. 요청/응답
	KindRealtime  Kind = "realtime"  // WebSocket 구독. 채널을 준다
	KindCondition Kind = "condition" // WebSocket 요청/응답
)
```

`Group` 에:

```go
	Kind Kind // 생성 템플릿. 빈 값은 KindREST 로 본다(기존 25줄을 고치지 않으려고)
```

그리고 헬퍼:

```go
// Template 은 이 그룹의 Kind 를 돌려준다. 빈 값은 REST 다.
func (g Group) Template() Kind {
	if g.Kind == "" {
		return KindREST
	}
	return g.Kind
}

// TemplateName 은 같은 값을 string 으로 돌려준다.
//
// text/template 의 eq 는 **타입이 다르면 비교하지 못한다** — 템플릿 안의 "rest" 는
// string 이고 Kind 는 아니라서, .Template 로 비교하면 실행 시점에 터진다.
// 템플릿에서는 반드시 이쪽을 쓴다.
func (g Group) TemplateName() string { return string(g.Template()) }
```

**기존 25줄은 고치지 않는다** — 빈 Kind 가 REST 로 읽힌다. 위치 기반 리터럴이 25번 바뀌는 것을 피한다.

- [ ] **Step 2: 실시간 템플릿 테스트를 쓴다**

`tools/gen/render_realtime_test.go`:

```go
package gen

import (
	"go/parser"
	"go/token"
	"strings"
	"testing"
)

func TestRenderRealtime_FID_를_json_태그로_쓴다(t *testing.T) {
	src, err := RenderRealtime(RealtimeTarget{
		Package: "realtime",
		GoName:  "SubscribeDomesticStockTrade",
		Type:    "0B",
		APIName: "주식체결",
		Fields: []RealtimeField{
			{FID: "10", Name: "CurrentPrice", Korean: "현재가"},
			{FID: "20", Name: "TradeTime", Korean: "체결시간"},
		},
	})
	if err != nil {
		t.Fatalf("RenderRealtime: %v", err)
	}
	got := string(src)

	if _, err := parser.ParseFile(token.NewFileSet(), "x.go", got, parser.AllErrors); err != nil {
		t.Fatalf("생성물이 Go 로 파싱되지 않는다: %v\n%s", err, got)
	}
	for _, want := range []string{
		"`json:\"10\"`",
		"CurrentPrice string",
		"// 현재가",
		`Raw map[string]string`,
		`"0B"`,
	} {
		if !strings.Contains(got, want) {
			t.Errorf("생성물에 %q 가 없다:\n%s", want, got)
		}
	}
}

func TestRenderRealtime_Raw_를_항상_채운다(t *testing.T) {
	src, err := RenderRealtime(RealtimeTarget{
		Package: "realtime", GoName: "SubscribeX", Type: "0X", APIName: "X",
		Fields: []RealtimeField{{FID: "1", Name: "A", Korean: "가"}},
	})
	if err != nil {
		t.Fatalf("RenderRealtime: %v", err)
	}
	// 모르는 FID 를 버리지 않으려면 원문을 맵으로도 한 번 더 풀어야 한다.
	if n := strings.Count(string(src), "json.Unmarshal(d.Values"); n != 2 {
		t.Errorf("values 를 %d번 푼다, 기대 2번(구조체 + Raw 맵)", n)
	}
}
```

- [ ] **Step 3: 실패를 확인한다**

```bash
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go/tools
go test ./gen/ -run Realtime
```
Expected: FAIL — `undefined: RenderRealtime`

- [ ] **Step 4: 실시간 템플릿을 쓴다**

`tools/gen/render_realtime.go`:

```go
package gen

import (
	"bytes"
	"fmt"
	"go/format"
	"text/template"
)

// RealtimeField 는 값 구조체의 필드 하나다.
type RealtimeField struct {
	FID    string
	Name   string
	Korean string
}

// RealtimeTarget 은 실시간 한 종류(파일 하나)다.
type RealtimeTarget struct {
	Package string
	GoName  string // 공식 영문명에서 온 메서드 이름
	Type    string // 실시간 항목 코드 (0B 등)
	APIName string
	Fields  []RealtimeField
}

// ValueType 은 값 구조체 이름이다. 메서드 이름에서 Subscribe 를 뗀다.
//
// SubscribeDomesticStockTrade 의 값이 SubscribeDomesticStockTradeValue 가 되면
// 읽기 어렵다. 접두어만 떼면 DomesticStockTrade 가 되어 자연스럽다.
func (t RealtimeTarget) ValueType() string {
	if len(t.GoName) > 9 && t.GoName[:9] == "Subscribe" {
		return t.GoName[9:]
	}
	return t.GoName + "Value"
}

// RenderRealtime 은 실시간 한 종류의 값 구조체와 구독 메서드를 만든다.
func RenderRealtime(t RealtimeTarget) ([]byte, error) {
	var buf bytes.Buffer
	if err := realtimeTmpl.Execute(&buf, t); err != nil {
		return nil, err
	}
	src, err := format.Source(buf.Bytes())
	if err != nil {
		return nil, fmt.Errorf("gofmt 실패(%s): %w\n%s", t.GoName, err, buf.String())
	}
	return src, nil
}

var realtimeTmpl = template.Must(template.New("realtime").Parse(
	`// Code generated by tools/gen. DO NOT EDIT.

package {{.Package}}

import (
	"context"
	"encoding/json"

	"github.com/kenshin579/kiwoom-go/internal/wstransport"
)

// {{.ValueType}} 는 {{.APIName}}({{.Type}}) 의 실시간 값이다.
//
// 필드 이름은 tools/gen/fids.go 의 표에서 온다 — 스펙은 FID 숫자와 한글명만 준다.
type {{.ValueType}} struct {
{{range .Fields}}	// {{.Korean}}
	{{.Name}} string ` + "`json:\"{{.FID}}\"`" + `
{{end}}}

// {{.GoName}} 은 {{.APIName}}({{.Type}}) 을 구독한다.
//
// ctx 를 취소하면 등록을 해제하고 채널을 닫는다. 채널이 가득 차면 이벤트를 버리고
// *kiwoom.SlowConsumerError 를 흘린다 — 한 구독의 느림이 다른 구독을 굶기지 않는다.
// 끊겼다 붙으면 *kiwoom.GapError 가 온다. 그 사이 이벤트는 받지 못한 것이다.
func (c *Client) {{.GoName}}(ctx context.Context, items ...string) (<-chan Event[{{.ValueType}}], error) {
	src, err := c.ws.Subscribe(ctx, "{{.Type}}", items, defaultBuffer)
	if err != nil {
		return nil, err
	}
	out := make(chan Event[{{.ValueType}}], defaultBuffer)
	go func() {
		defer close(out)
		for d := range src {
			out <- decode{{.ValueType}}(d)
		}
	}()
	return out, nil
}

func decode{{.ValueType}}(d wstransport.Delivery) Event[{{.ValueType}}] {
	ev := Event[{{.ValueType}}]{Symbol: d.Item, Name: d.Name, Time: d.Time, Err: d.Err}
	if d.Err != nil {
		return ev
	}
	// 두 번 푼다. 구조체는 표에 있는 FID 만 받고, Raw 는 전부 받는다 —
	// 키움이 FID 를 추가했을 때 encoding/json 이 조용히 버리지 않게 한다.
	if err := json.Unmarshal(d.Values, &ev.Value); err != nil {
		ev.Err = err
		return ev
	}
	if err := json.Unmarshal(d.Values, &ev.Raw); err != nil {
		ev.Err = err
	}
	return ev
}
`))
```

- [ ] **Step 5: 조건검색 템플릿 테스트를 쓴다**

`tools/gen/render_condition_test.go`:

```go
package gen

import (
	"go/parser"
	"go/token"
	"strings"
	"testing"
)

func TestRenderCondition_trnm_으로_요청한다(t *testing.T) {
	src, err := RenderCondition(Target{
		Package:  "condition",
		GoName:   "ListDomesticConditionSearches",
		APIID:    "ka10171",
		APIName:  "조건검색 목록조회",
		MenuPath: "국내주식 > 조건검색 > 조건검색 목록조회(ka10171)",
		Path:     "/api/dostk/websocket",
		Request:  []Node{{Field: Field{Element: "trnm", Korean: "TR명", Type: "String", Required: "Y"}}},
		Response: []Node{{Field: Field{Element: "return_code", Korean: "결과코드", Type: "String"}}},
	}, "CNSRLST")
	if err != nil {
		t.Fatalf("RenderCondition: %v", err)
	}
	got := string(src)
	if _, err := parser.ParseFile(token.NewFileSet(), "x.go", got, parser.AllErrors); err != nil {
		t.Fatalf("생성물이 Go 로 파싱되지 않는다: %v\n%s", err, got)
	}
	for _, want := range []string{`"CNSRLST"`, "c.ws.Request(ctx", "ListDomesticConditionSearchesRequest"} {
		if !strings.Contains(got, want) {
			t.Errorf("생성물에 %q 가 없다:\n%s", want, got)
		}
	}
}
```

- [ ] **Step 6: 조건검색 템플릿을 쓴다**

`tools/gen/render_condition.go`:

```go
package gen

import (
	"bytes"
	"fmt"
	"go/format"
	"text/template"
)

// conditionTarget 은 조건검색 한 건이다. Target 에 trnm 만 더한 것이다.
type conditionTarget struct {
	Target
	Trnm string
}

// RenderCondition 은 조건검색 요청/응답 한 건을 만든다.
//
// REST 와 모양이 거의 같지만 전송이 WebSocket 이고, 짝짓기 열쇠가 api-id 가 아니라
// trnm 이다. 연속조회 헤더도 없다 — 이어보기는 요청 본문의 cont_yn/next_key 로 한다.
func RenderCondition(t Target, trnm string) ([]byte, error) {
	var buf bytes.Buffer
	if err := conditionTmpl.Execute(&buf, conditionTarget{Target: t, Trnm: trnm}); err != nil {
		return nil, err
	}
	src, err := format.Source(buf.Bytes())
	if err != nil {
		return nil, fmt.Errorf("gofmt 실패(%s): %w\n%s", t.GoName, err, buf.String())
	}
	return src, nil
}

var conditionTmpl = template.Must(template.New("condition").Funcs(template.FuncMap{
	"structs": structs,
}).Parse(`// Code generated by tools/gen. DO NOT EDIT.

package {{.Package}}

import "context"

{{- $req := structs (printf "%sRequest" .GoName) .Request}}
{{- $res := structs .GoName .Response}}

// {{.GoName}}Request 는 {{.APIName}}({{.APIID}}) 요청이다.
type {{.GoName}}Request struct {
{{index $req 0}}}

// {{.GoName}}Response 는 {{.APIName}}({{.APIID}}) 응답이다.
type {{.GoName}}Response struct {
{{index $res 0}}}
{{index $res 1}}
// {{.GoName}} 는 {{.APIName}}({{.APIID}}) 이다. WebSocket 위의 요청/응답이다.
//
// 메뉴: {{.MenuPath}}
// URL:  {{.Path}}  (trnm: {{.Trnm}})
func (c *Client) {{.GoName}}(ctx context.Context, req {{.GoName}}Request) (*{{.GoName}}Response, error) {
	var out {{.GoName}}Response
	if err := c.ws.Request(ctx, "{{.Trnm}}", req, &out); err != nil {
		return nil, err
	}
	return &out, nil
}
`))
```

- [ ] **Step 7: 하위 클라이언트 템플릿에 WS 종류를 더한다**

`tools/gen/render_client.go` 의 `RenderSubClient` 를 `Kind` 로 갈라지게 고친다:

```go
// RenderSubClient 는 그룹 하나의 client.go 를 만든다.
func RenderSubClient(g Group) ([]byte, error) {
	tmpl := subClientTmpl
	if g.Template() != KindREST {
		tmpl = wsSubClientTmpl
	}
	var buf bytes.Buffer
	if err := tmpl.Execute(&buf, g); err != nil {
		return nil, err
	}
	src, err := format.Source(buf.Bytes())
	if err != nil {
		return nil, fmt.Errorf("gofmt 실패(%s): %w\n%s", g.Pkg, err, buf.String())
	}
	return src, nil
}
```

그리고 새 템플릿을 더한다. 실시간 패키지만 `Event`·`defaultBuffer` 가 필요하므로 `Kind` 로 한 번 더 가른다:

실시간과 조건검색이 **둘 다** `Event` 를 쓰므로 분기하지 않는다:

```go
var wsSubClientTmpl = template.Must(template.New("wssubclient").Parse(
	`// Code generated by tools/gen. DO NOT EDIT.

// Package {{.Pkg}} 는 키움 {{.Korean}} API 그룹이다. WebSocket 위에서 돈다.
package {{.Pkg}}

import (
	"time"

	"github.com/kenshin579/kiwoom-go/internal/wstransport"
)

// defaultBuffer 는 구독 채널 버퍼 크기다.
//
// 가득 차면 이벤트를 버리고 *kiwoom.SlowConsumerError 를 흘린다. 넉넉히 잡는 이유는
// 0D(주식호가잔량)처럼 초당 여러 건이 오는 종류가 있기 때문이다.
const defaultBuffer = 256

// Event 는 실시간 한 건이다. 루트의 kiwoom.Event 와 같은 모양이다.
//
// 같은 타입을 두 곳에 두는 이유: 루트가 이 패키지를 import 하므로(subclients.go),
// 이 패키지는 루트를 import 할 수 없다. 루트의 것은 별칭이 아니라 같은 정의다.
type Event[T any] struct {
	Symbol string
	Name   string
	Time   time.Time
	Value  T
	Raw    map[string]string
	Err    error
}

// Client 는 {{.Korean}} 하위 클라이언트다.
type Client struct {
	ws *wstransport.Conn
}

// New 는 kiwoom.NewClient 가 호출한다.
func New(ws *wstransport.Conn) *Client { return &Client{ws: ws} }
`))
```

- [ ] **Step 8: 통과를 확인한다**

```bash
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go/tools
go test ./gen/ -count=1 && go vet ./... && gofmt -l .
```
Expected: 전부 PASS/조용

- [ ] **Step 9: 커밋**

```bash
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go
git add tools/gen/groups.go tools/gen/render_client.go tools/gen/render_realtime.go tools/gen/render_realtime_test.go tools/gen/render_condition.go tools/gen/render_condition_test.go
git commit -m "feat(gen): Kind 별 템플릿 — 실시간과 조건검색" -- tools/gen/groups.go tools/gen/render_client.go tools/gen/render_realtime.go tools/gen/render_realtime_test.go tools/gen/render_condition.go tools/gen/render_condition_test.go
```

---

## Task 9: 배선하고 31개를 생성한다

**Files:**
- Modify: `tools/gen/groups.go` (표 4줄 추가, `skipped` 4줄 제거)
- Modify: `tools/gen/render_client.go` (`RenderClients` 가 전송 셋을 받게)
- Modify: `tools/gen/main/main.go`
- Modify: `tools/gen/golden_test.go`
- Modify: `subclients_test.go`
- Create: 생성물 — `domestic/realtime/`, `overseas/realtime/`, `domestic/condition/`, `overseas/condition/`

- [ ] **Step 1: 표를 고친다**

`tools/gen/groups.go` 의 `Groups` 끝(미국주식 줄들 뒤)에 4줄을 더한다:

```go
	{Menu: "국내주식 > 실시간시세", Market: "domestic", Pkg: "realtime", Field: "DomesticRealtime", Korean: "국내주식 실시간시세", Kind: KindRealtime},
	{Menu: "국내주식 > 조건검색", Market: "domestic", Pkg: "condition", Field: "DomesticCondition", Korean: "국내주식 조건검색", Kind: KindCondition},
	{Menu: "미국주식 > 실시간시세", Market: "overseas", Pkg: "realtime", Field: "OverseasRealtime", Korean: "미국주식 실시간시세", Kind: KindRealtime},
	{Menu: "미국주식 > 조건검색", Market: "overseas", Pkg: "condition", Field: "OverseasCondition", Korean: "미국주식 조건검색", Kind: KindCondition},
```

기존 25줄은 위치 기반 리터럴이고 새 4줄은 필드 이름을 쓴다. Go 는 한 리터럴 안에서 두 방식을 섞지 못하지만 **원소마다는 다르게 써도 된다.** 새 줄만 이름을 쓰는 이유는 `Kind` 를 건너뛰기 위해서다.

`skipped` 에서 실시간·조건검색 4줄을 지운다. OAuth 2줄은 남긴다:

```go
var skipped = map[string]bool{
	"OAuth 인증 > 접근토큰발급": true,
	"OAuth 인증 > 접근토큰폐기": true,
}
```

`skipped` 의 doc 주석에서 "실시간·조건검색은 WebSocket 이라 프로토콜이 다르고(3단계)" 문장을 지우고 이렇게 바꾼다:

```go
// skipped 는 **일부러** 생성하지 않는 카테고리다.
//
// OAuth 는 internal/auth 가 손으로 다룬다. 여기 적어 두는 이유는 Groups 에 없는
// 카테고리를 전부 에러로 잡기 위해서다 — 그래야 새 카테고리가 조용히 빠지지 않는다.
```

- [ ] **Step 2: `RenderClients` 가 전송 셋을 받게 한다**

`tools/gen/render_client.go` 의 `clientsTmpl` 에서 `newClients` 부분을 고친다:

```
// newClients 는 NewClient 가 호출한다.
func newClients(tr *transport.Client, dom, ovs *wstransport.Conn) Clients {
	return Clients{
{{range .}}		{{.Field}}: {{.Alias}}.New({{if eq .TemplateName "rest"}}tr{{else if eq .Market "domestic"}}dom{{else}}ovs{{end}}),
{{end}}	}
}
```

import 블록에 `wstransport` 를 더한다:

```
import (
	"github.com/kenshin579/kiwoom-go/internal/transport"
	"github.com/kenshin579/kiwoom-go/internal/wstransport"
{{range .}}	{{.Alias}} "{{.ImportPath}}"
{{end}})
```

`Template` 은 `Kind` 를 돌려주므로 템플릿에서 문자열 비교가 된다. `RenderClients` 의 기존 테스트(`tools/gen/render_client_test.go`)에서 `newClients(tr)` 를 기대하는 곳이 있으면 새 시그니처로 고친다.

- [ ] **Step 3: `main.go` 가 Kind 로 갈라지게 한다**

`tools/gen/main/main.go` 의 API 루프에서 `gen.Render(...)` 호출부를 이렇게 바꾼다:

```go
		var code []byte
		switch g.Template() {
		case gen.KindRealtime:
			fields, ferr := gen.RealtimeFields(api)
			if ferr != nil {
				log.Fatalf("%s(%s) FID: %v", id, menu, ferr)
			}
			code, err = gen.RenderRealtime(gen.RealtimeTarget{
				Package: g.Pkg,
				GoName:  gen.GoName(name),
				Type:    id,
				APIName: api.Meta["API 명"],
				Fields:  fields,
			})
		case gen.KindCondition:
			trnm, terr := gen.ConditionTrnm(api)
			if terr != nil {
				log.Fatalf("%s(%s) trnm: %v", id, menu, terr)
			}
			code, err = gen.RenderCondition(gen.Target{
				Package:  g.Pkg,
				GoName:   gen.GoName(name),
				APIID:    id,
				APIName:  api.Meta["API 명"],
				MenuPath: menu,
				Path:     api.Meta["URL"],
				Request:  reqTree,
				Response: resTree,
			}, trnm)
		default:
			code, err = gen.Render(gen.Target{
				Package:  g.Pkg,
				GoName:   gen.GoName(name),
				APIID:    id,
				APIName:  api.Meta["API 명"],
				MenuPath: menu,
				Path:     api.Meta["URL"],
				Request:  reqTree,
				Response: resTree,
			})
		}
		if err != nil {
			log.Fatalf("%s 렌더: %v", id, err)
		}
```

**주의:** `BuildTree` 는 `ka10173` 에서 에러를 낸다(`Object`·`List<Map>`). 그래서 실시간·조건검색은 트리를 다르게 다뤄야 한다. `gen.RealtimeFields` 는 트리를 쓰지 않고 평평한 필드에서 depth-2 만 뽑는다. `ka10173`·`usa20290` 은 Step 4 에서 생성 대상에서 뺀다.

- [ ] **Step 4: 두 헬퍼와 손으로 쓸 목록을 더한다**

`tools/gen/fids.go` 끝에 더한다:

```go
// handWritten 은 생성하지 않고 손으로 쓰는 API 다.
//
// ka10173 은 응답이 두 벌이다(조회 결과 + REAL 푸시). 스펙에서도 이것만 is_section 으로
// 본문이 갈라진다. 337개 중 하나를 위해 생성기에 분기를 넣는 것보다, 하나를 손으로 쓰고
// 생성기를 단순하게 두는 편이 낫다.
//
// usa20290 은 푸시가 오지만 FID 표가 스펙에 비어 있다(SOURCE.md 참고). 조회는 타입을
// 붙이고 푸시는 Raw 맵으로만 내므로 역시 손으로 쓴다.
var handWritten = map[string]bool{
	"ka10173":  true,
	"usa20290": true,
}

// HandWritten 은 생성 대상에서 빼고 손으로 쓰는 API 인지 알려준다.
func HandWritten(apiID string) bool { return handWritten[apiID] }

// RealtimeFields 는 실시간 응답의 depth-2 FID 필드를 표와 맞춰 돌려준다.
//
// BuildTree 를 거치지 않는다 — 실시간 응답의 values 는 맵이라 LIST 트리로 접을 수 없다
// (설계 §7). 평평한 필드에서 depth 2 만 순서대로 뽑는다.
func RealtimeFields(api API) ([]RealtimeField, error) {
	var out []RealtimeField
	seen := map[string]bool{}
	for _, f := range api.Response.Body {
		if f.Depth != 2 || !isDigitString(f.Element) || seen[f.Element] {
			continue
		}
		seen[f.Element] = true
		fid, ok := LookupFid(f.Element)
		if !ok {
			return nil, fmt.Errorf(
				"표에 없는 FID: %q(%s). tools/gen/fids.go 에 넣어라", f.Element, f.Korean)
		}
		out = append(out, RealtimeField{FID: fid.FID, Name: fid.Name, Korean: f.Korean})
	}
	return out, nil
}

// ConditionTrnm 은 조건검색 요청의 trnm 고정값을 스펙 설명에서 뽑는다.
//
// 지어내지 않고 스펙에서 읽는 이유: 미국 쪽은 G 접두어가 붙고(GCNSRLST), 스펙에 두 값이
// 적힌 자리가 있어 손으로 옮기면 틀리기 쉽다.
func ConditionTrnm(api API) (string, error) {
	for _, f := range api.Request.Body {
		if f.Element != "trnm" {
			continue
		}
		// "CNSRLST 고정값" · "CNSRLST고정값" · "GCNSRREQ" 모두에서 앞의 대문자 토큰을 뗀다.
		for _, r := range f.Description {
			if r < 'A' || r > 'Z' {
				break
			}
		}
		var b []rune
		for _, r := range f.Description {
			if r >= 'A' && r <= 'Z' {
				b = append(b, r)
				continue
			}
			break
		}
		if len(b) > 0 {
			return string(b), nil
		}
	}
	return "", fmt.Errorf("요청에서 trnm 고정값을 찾지 못했다")
}

func isDigitString(s string) bool {
	if s == "" {
		return false
	}
	for _, r := range s {
		if r < '0' || r > '9' {
			return false
		}
	}
	return true
}
```

`"fmt"` 를 `fids.go` 의 import 에 넣는다.

`main.go` 의 루프 맨 앞(`gen.Lookup` 직후)에 한 줄을 더한다:

```go
		if gen.HandWritten(id) {
			continue
		}
```

`BuildTree` 호출도 `ka10173` 을 피해야 하므로 이 `continue` 가 그 앞에 와야 한다.

- [ ] **Step 5: 생성한다**

```bash
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go/tools
go build ./... && go run ./gen/main
```

기대 출력의 새 줄. **다르면 멈추고 보고하라:**

```
domestic/realtime         19개
domestic/condition         3개
overseas/realtime          4개
overseas/condition         3개
합계                      333개
```

`304 + 19 + 3 + 4 + 3 = 333` 이다. 손으로 쓸 `ka10173`·`usa20290` 2개가 빠져 337 − 2(OAuth) − 2(손) = 333.

- [ ] **Step 6: 배선 테스트를 고친다**

`subclients_test.go` 의 개수 계약을 25 → 29 로 고친다:

```go
	const want = 29
```

주석에 한 줄 더한다:

```go
	// 3단계에서 실시간 2 + 조건검색 2 = 4개가 늘어 25 → 29 가 됐다.
```

- [ ] **Step 7: 골든 테스트를 고친다**

`tools/gen/golden_test.go` 의 `renderAll` 이 `main.go` 와 같은 갈래를 타게 한다. `gen.Render(...)` 호출부를 Step 3 과 **같은 `switch`** 로 바꾸고, `gen.Lookup` 직후에 `if gen.HandWritten(id) { continue }` 를 더한다. `t.Fatalf` 로 실패를 낸다(`log.Fatalf` 아님).

`renderAll` 끝의 `gen.RenderClients(gen.Groups)` 는 그대로다.

- [ ] **Step 8: 컴파일과 검사**

```bash
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go
gofmt -l .
go build ./... && go vet ./... && go test ./... -count=1
cd tools && go build ./... && go vet ./... && go test ./... -count=1 && gofmt -l .
```
Expected: 전부 통과. 루트가 Task 5 이후 처음으로 붙는다.

**깨지면 생성기를 고쳐 다시 돌려라. 생성물을 손으로 고치지 마라.**

- [ ] **Step 9: 재생성 결정성을 확인한다**

```bash
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go
git status --porcelain > /tmp/p3_run1.txt
cd tools && go run ./gen/main >/dev/null
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go
git status --porcelain > /tmp/p3_run2.txt
diff /tmp/p3_run1.txt /tmp/p3_run2.txt && echo "결정적"
```
Expected: `결정적`

- [ ] **Step 10: 커밋 — 셋으로 나눈다**

```bash
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go

git add tools/gen/groups.go tools/gen/fids.go tools/gen/render_client.go tools/gen/main/main.go tools/gen/golden_test.go
git commit -m "feat(gen): 실시간·조건검색을 생성 대상에 넣는다" -- tools/gen/groups.go tools/gen/fids.go tools/gen/render_client.go tools/gen/main/main.go tools/gen/golden_test.go

git add subclients.go subclients_test.go
git commit -m "feat: 하위 클라이언트 29개로 배선" -- subclients.go subclients_test.go

git add domestic overseas
git commit -m "feat: 실시간 23개와 조건검색 6개 생성" -- domestic overseas
```

---

## Task 10: `ka10173` 과 `usa20290` 을 손으로 쓴다

**Files:**
- Create: `domestic/condition/realtime_condition_search.go`
- Create: `overseas/condition/realtime_condition_search.go`
- Create: `domestic/condition/realtime_condition_search_test.go`

이 파일들에는 **생성물 헤더를 붙이지 않는다.** 붙이면 `cleanGenerated` 가 지우고 골든 테스트가 orphan 으로 잡는다.

- [ ] **Step 1: 국내(`ka10173`)를 쓴다**

`domestic/condition/realtime_condition_search.go`:

```go
package condition

import (
	"context"
	"encoding/json"

	"github.com/kenshin579/kiwoom-go/internal/wstransport"
)

// 손으로 쓴 파일이다. 생성하지 않는다 — tools/gen/fids.go 의 handWritten 참고.
//
// ka10173 은 337개 중 유일하게 **응답이 두 벌**이다. 조회 결과가 한 번 오고, 그 뒤로
// REAL 푸시가 계속 온다. 스펙에서도 이것만 is_section 으로 본문이 갈라진다.
// 이 하나를 위해 생성기에 분기를 넣는 것보다, 하나를 손으로 쓰고 생성기를 단순하게
// 두는 편이 낫다.

// RequestDomesticRealtimeConditionSearchRequest 는 조건검색 요청 실시간(ka10173) 요청이다.
type RequestDomesticRealtimeConditionSearchRequest struct {
	Trnm       string `json:"trnm"`        // 서비스명 (필수, CNSRREQ 고정값)
	Seq        string `json:"seq"`         // 조건검색식 일련번호 (필수)
	SearchType string `json:"search_type"` // 조회타입 (필수, 1: 조건검색+실시간조건검색)
	StexTp     string `json:"stex_tp"`     // 거래소구분 (필수, K:KRX)
}

// RequestDomesticRealtimeConditionSearchResponse 는 조회 응답이다(푸시 이전 한 번).
type RequestDomesticRealtimeConditionSearchResponse struct {
	ReturnCode int    `json:"return_code"` // 결과코드 (정상:0)
	ReturnMsg  string `json:"return_msg"`  // 결과메시지
	Trnm       string `json:"trnm"`        // 서비스명
	Seq        string `json:"seq"`         // 조건검색식 일련번호
	Data       []struct {
		Jmcode string `json:"jmcode"` // 종목코드 (접두어 1자리 + 종목코드 6자리)
	} `json:"data"`
}

// DomesticRealtimeConditionMatch 는 조건검색 편입·이탈 푸시 한 건이다.
//
// 필드 이름은 tools/gen/fids.go 의 표에서 왔다 — 생성물과 같은 출처다.
type DomesticRealtimeConditionMatch struct {
	Sequence   string `json:"841"`  // 일련번호
	Symbol     string `json:"9001"` // 종목코드
	InsertDrop string `json:"843"`  // 삽입삭제 구분 (I: 삽입, D: 삭제)
	TradeTime  string `json:"20"`   // 체결시간
	SellBuy    string `json:"907"`  // 매도/수 구분
}

// RequestDomesticRealtimeConditionSearch 는 조건검색 요청 실시간(ka10173) 이다.
//
// 조회 응답을 돌려주고, 그 뒤의 편입·이탈 푸시를 채널로 흘린다. ctx 를 취소하면
// 채널이 닫힌다 — 실시간 해제는 StopDomesticRealtimeConditionSearch(ka10174)로 따로 보낸다.
//
// 메뉴: 국내주식 > 조건검색 > 조건검색 요청 실시간(ka10173)
// URL:  /api/dostk/websocket  (trnm: CNSRREQ)
func (c *Client) RequestDomesticRealtimeConditionSearch(
	ctx context.Context, req RequestDomesticRealtimeConditionSearchRequest,
) (*RequestDomesticRealtimeConditionSearchResponse, <-chan Event[DomesticRealtimeConditionMatch], error) {
	var out RequestDomesticRealtimeConditionSearchResponse
	if err := c.ws.Request(ctx, "CNSRREQ", req, &out); err != nil {
		return nil, nil, err
	}

	// 푸시는 실시간과 같은 경로로 온다 — type 은 조건검색식 일련번호가 아니라 "0B" 등이
	// 아니므로, 여기서는 seq 를 열쇠로 삼는다.
	src, err := c.ws.SubscribeCondition(ctx, out.Seq, defaultBuffer)
	if err != nil {
		return &out, nil, err
	}

	ch := make(chan Event[DomesticRealtimeConditionMatch], defaultBuffer)
	go func() {
		defer close(ch)
		for d := range src {
			ch <- decodeConditionMatch(d)
		}
	}()
	return &out, ch, nil
}

func decodeConditionMatch(d wstransport.Delivery) Event[DomesticRealtimeConditionMatch] {
	ev := Event[DomesticRealtimeConditionMatch]{
		Symbol: d.Item, Name: d.Name, Time: d.Time, Err: d.Err,
	}
	if d.Err != nil {
		return ev
	}
	if err := json.Unmarshal(d.Values, &ev.Value); err != nil {
		ev.Err = err
		return ev
	}
	if err := json.Unmarshal(d.Values, &ev.Raw); err != nil {
		ev.Err = err
	}
	return ev
}
```

**`SubscribeCondition` 은 아직 없다.** Step 2 에서 만든다.

- [ ] **Step 2: `wstransport` 에 조건검색 푸시 구독을 더한다**

조건검색 푸시는 `trnm: "REAL"` 로 오지만 `data[].type` 이 실시간 항목 코드가 아니다. 종목별 구독이 아니라 **조건검색식 단위**다.

`internal/wstransport/sub.go` 끝에 더한다:

```go
// conditionKey 는 조건검색 푸시의 라우팅 열쇠다.
//
// 실시간(type+item)과 달리 조건검색은 조건검색식 일련번호 하나로 갈린다.
func conditionKey(seq string) subKey { return subKey{typ: "__cond", item: seq} }

// SubscribeCondition 은 조건검색 편입·이탈 푸시를 받는다.
//
// 등록 패킷을 보내지 않는다 — 조건검색은 CNSRREQ 요청 자체가 등록이다. 여기서는
// 라우팅만 붙인다. 해제는 CNSRCLR 요청으로 따로 보낸다(ka10174).
func (c *Conn) SubscribeCondition(ctx context.Context, seq string, buf int) (<-chan Delivery, error) {
	if buf < 1 {
		buf = 1
	}
	ch := make(chan Delivery, buf)
	s := &sub{key: conditionKey(seq), ch: ch}

	c.subMu.Lock()
	if c.subs == nil {
		c.subs = map[subKey][]*sub{}
	}
	c.subs[s.key] = append(c.subs[s.key], s)
	c.subMu.Unlock()

	go func() {
		<-ctx.Done()
		c.drop([]*sub{s})
		close(ch)
	}()
	return ch, nil
}
```

`routeReal` 이 조건검색 푸시도 나눠 주도록 고친다. 편입·이탈 푸시는 `values` 에 `841`(일련번호)이 있고 `type` 이 실시간 코드가 아니다:

```go
	for _, p := range payloads {
		keys := []subKey{{p.Type, p.Item}}
		// 조건검색 푸시는 조건검색식 일련번호로도 라우팅한다. values 의 841 이 그것이다.
		if seq := fieldOf(p.Values, "841"); seq != "" {
			keys = append(keys, conditionKey(seq))
		}
		for _, k := range keys {
			c.subMu.Lock()
			list := append([]*sub(nil), c.subs[k]...)
			c.subMu.Unlock()
			for _, s := range list {
				c.deliver(s, Delivery{
					Type: p.Type, Name: p.Name, Item: p.Item,
					Values: p.Values, Time: now,
				})
			}
		}
	}
```

그리고 헬퍼:

```go
// fieldOf 는 values 에서 FID 하나를 문자열로 꺼낸다. 없으면 빈 문자열이다.
func fieldOf(values json.RawMessage, fid string) string {
	var m map[string]string
	if err := json.Unmarshal(values, &m); err != nil {
		return ""
	}
	return m[fid]
}
```

- [ ] **Step 3: 미국(`usa20290`)을 쓴다**

`overseas/condition/realtime_condition_search.go`:

```go
package condition

import (
	"context"
	"encoding/json"
)

// 손으로 쓴 파일이다. 생성하지 않는다 — tools/gen/fids.go 의 handWritten 참고.
//
// usa20290 은 푸시가 실제로 오지만 **스펙에 푸시 필드가 하나도 없다.** 공식 예제도
// COLUMNS 를 비워 두고 "수동 생성 필요" 마커를 달았다(tools/spec/SOURCE.md 참고).
//
// 그래서 조회 응답은 타입을 붙이고, 푸시는 Raw 맵으로만 낸다. FID 이름을 지어내지
// 않는다 — 스펙에 없는 것을 있는 것처럼 만들지 않는다. 키움이 문서를 채우면 그때
// 생성 대상으로 옮기면 된다.

// RequestOverseasRealtimeConditionSearchRequest 는 미국주식 조건검색 요청 실시간(usa20290) 요청이다.
type RequestOverseasRealtimeConditionSearchRequest struct {
	Trnm       string `json:"trnm"`        // 서비스명 (필수, GCNSRREQ 고정값)
	Seq        string `json:"seq"`         // 조건검색식 일련번호 (필수)
	SearchType string `json:"search_type"` // 조회타입 (필수, 1: 조건검색+실시간조건검색)
}

// RequestOverseasRealtimeConditionSearchResponse 는 조회 응답이다(푸시 이전 한 번).
type RequestOverseasRealtimeConditionSearchResponse struct {
	ReturnCode int    `json:"return_code"` // 결과코드 (정상:0)
	ReturnMsg  string `json:"return_msg"`  // 결과메시지
	Trnm       string `json:"trnm"`        // 서비스명
	Seq        string `json:"seq"`         // 조건검색식 일련번호
	Data       []struct {
		Jmcode string `json:"jmcode"`  // 종목코드
		StexTp string `json:"stexTp"`  // 거래소구분
	} `json:"data"`
}

// RequestOverseasRealtimeConditionSearch 는 미국주식 조건검색 요청 실시간(usa20290) 이다.
//
// 조회 응답은 타입이 붙지만 **푸시는 Raw 맵뿐이다** — 스펙에 FID 이름표가 없다.
// Event.Raw 에 받은 FID 가 전부 들어 있다. Event.Value 는 항상 비어 있다.
//
// 메뉴: 미국주식 > 조건검색 > 미국주식 조건검색 요청 실시간(usa20290)
// URL:  /api/us/websocket  (trnm: GCNSRREQ)
func (c *Client) RequestOverseasRealtimeConditionSearch(
	ctx context.Context, req RequestOverseasRealtimeConditionSearchRequest,
) (*RequestOverseasRealtimeConditionSearchResponse, <-chan Event[struct{}], error) {
	var out RequestOverseasRealtimeConditionSearchResponse
	if err := c.ws.Request(ctx, "GCNSRREQ", req, &out); err != nil {
		return nil, nil, err
	}

	src, err := c.ws.SubscribeCondition(ctx, out.Seq, defaultBuffer)
	if err != nil {
		return &out, nil, err
	}

	ch := make(chan Event[struct{}], defaultBuffer)
	go func() {
		defer close(ch)
		for d := range src {
			ev := Event[struct{}]{Symbol: d.Item, Name: d.Name, Time: d.Time, Err: d.Err}
			if d.Err == nil {
				if err := json.Unmarshal(d.Values, &ev.Raw); err != nil {
					ev.Err = err
				}
			}
			ch <- ev
		}
	}()
	return &out, ch, nil
}
```

- [ ] **Step 4: `condition` 패키지에 `Event` 가 있는지 확인한다**

Task 8 Step 7 의 `wsSubClientTmpl` 이 실시간·조건검색 **둘 다**에 `Event` 와 `defaultBuffer` 를 낸다. 확인만 한다:

```bash
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go
grep -n "defaultBuffer\|type Event" domestic/condition/client.go
```
Expected: `const defaultBuffer = 256` 과 `type Event[T any] struct` 둘 다 나온다. 없으면 Task 8 Step 7 을 다시 보라.

- [ ] **Step 5: 손으로 쓴 코드에 테스트를 붙인다**

`domestic/condition/realtime_condition_search_test.go`:

```go
package condition

import (
	"encoding/json"
	"testing"

	"github.com/kenshin579/kiwoom-go/internal/wstransport"
)

// TestDecodeConditionMatch_Raw_에_모르는_FID_도_남는다 는 표에 없는 FID 가 버려지지
// 않는지 본다. encoding/json 은 기본적으로 조용히 버린다.
func TestDecodeConditionMatch_Raw_에_모르는_FID_도_남는다(t *testing.T) {
	values := json.RawMessage(`{"841":"1","9001":"A005930","843":"I","20":"093015","907":"2","9999":"새필드"}`)
	ev := decodeConditionMatch(wstransport.Delivery{Item: "A005930", Values: values})

	if ev.Err != nil {
		t.Fatalf("Err = %v", ev.Err)
	}
	if ev.Value.Symbol != "A005930" || ev.Value.InsertDrop != "I" {
		t.Errorf("Value = %+v, 기대 Symbol=A005930 InsertDrop=I", ev.Value)
	}
	if ev.Raw["9999"] != "새필드" {
		t.Errorf("Raw[9999] = %q, 기대 새필드 — 모르는 FID 가 버려졌다", ev.Raw["9999"])
	}
}

// TestDecodeConditionMatch_에러는_값을_비운다 는 봉투 규약을 본다.
func TestDecodeConditionMatch_에러는_값을_비운다(t *testing.T) {
	want := &wstransport.SlowConsumerError{Type: "__cond", Item: "0", Dropped: 3}
	ev := decodeConditionMatch(wstransport.Delivery{Err: want})
	if ev.Err != want {
		t.Errorf("Err = %v, 기대 그대로 전달", ev.Err)
	}
	if ev.Value != (DomesticRealtimeConditionMatch{}) {
		t.Errorf("Value = %+v, 기대 영값", ev.Value)
	}
}
```

- [ ] **Step 6: 검사한다**

```bash
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go
gofmt -l .
go build ./... && go vet ./... && go test ./... -count=1
cd tools && go test ./... -count=1
```
Expected: 전부 통과. 골든 테스트가 **orphan 으로 걸리지 않아야 한다**(손으로 쓴 파일에는 생성물 헤더가 없다).

- [ ] **Step 7: 커밋**

```bash
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go
git add internal/wstransport domestic/condition overseas/condition domestic/realtime overseas/realtime tools/gen/render_client.go subclients.go
git commit -m "feat: 조건검색 실시간 두 건을 손으로 쓴다" -- internal/wstransport domestic/condition overseas/condition domestic/realtime overseas/realtime tools/gen/render_client.go subclients.go
```

---

## Task 11: README 와 전체 검증

**Files:**
- Modify: `README.md`
- Create: `stream_test.go`

- [ ] **Step 1: 외부 모듈에서 쓸 수 있는지 확인한다**

```bash
mkdir -p /tmp/kiwoom_p3 && cd /tmp/kiwoom_p3
cat > go.mod <<'EOF'
module example.com/p3

go 1.25

require github.com/kenshin579/kiwoom-go v0.0.0

replace github.com/kenshin579/kiwoom-go => /Users/frankoh/src/workspace_moneyflow/kiwoom-go
EOF
cat > main.go <<'EOF'
package main

import (
	"context"
	"errors"
	"fmt"

	"github.com/kenshin579/kiwoom-go"
	"github.com/kenshin579/kiwoom-go/domestic/realtime"
)

func main() {
	c, err := kiwoom.NewClient("AK", "SK", kiwoom.WithMock())
	if err != nil {
		panic(err)
	}
	ctx := context.Background()

	// 구독이 외부에서 만들어지는지
	ch, err := c.DomesticRealtime.SubscribeDomesticStockTrade(ctx, "005930")
	if err != nil {
		panic(err)
	}
	for ev := range ch {
		var gap *kiwoom.GapError
		if errors.As(ev.Err, &gap) {
			fmt.Println("구멍:", gap.Since, gap.Until)
			continue
		}
		// 값 타입과 Raw 가 외부에서 읽히는지
		var v realtime.DomesticStockTrade = ev.Value
		fmt.Println(ev.Symbol, v.CurrentPrice, ev.Raw["10"])
	}
}
EOF
go mod tidy && go build ./... && go vet ./... && echo "외부 모듈 빌드 성공"
rm -rf /tmp/kiwoom_p3
```

**실패하면 에러를 그대로 보고하라** (고치지 말고 먼저 보고하라 — 생성기나 별칭 문제일 수 있다).

- [ ] **Step 2: 루트 배선 테스트를 더한다**

`stream_test.go`:

```go
package kiwoom_test

import (
	"errors"
	"testing"
	"time"

	"github.com/kenshin579/kiwoom-go"
)

// TestGapError_는_errors_As_로_잡힌다 는 외부에서 구멍을 구분할 수 있는지 본다.
//
// 이걸 못 하면 사용자는 끊김과 다른 에러를 가르지 못하고, 결국 구멍을 무시하게 된다.
func TestGapError_는_errors_As_로_잡힌다(t *testing.T) {
	since := time.Now().Add(-time.Minute)
	until := time.Now()
	var err error = &kiwoom.GapError{Since: since, Until: until}

	var gap *kiwoom.GapError
	if !errors.As(err, &gap) {
		t.Fatal("errors.As 가 *kiwoom.GapError 를 잡지 못한다")
	}
	if !gap.Since.Equal(since) || !gap.Until.Equal(until) {
		t.Errorf("시각이 바뀌었다: %+v", gap)
	}
}

// TestSlowConsumerError_는_errors_As_로_잡힌다 도 같은 이유다.
func TestSlowConsumerError_는_errors_As_로_잡힌다(t *testing.T) {
	var err error = &kiwoom.SlowConsumerError{Type: "0B", Item: "005930", Dropped: 7}
	var slow *kiwoom.SlowConsumerError
	if !errors.As(err, &slow) {
		t.Fatal("errors.As 가 *kiwoom.SlowConsumerError 를 잡지 못한다")
	}
	if slow.Dropped != 7 {
		t.Errorf("Dropped = %d, 기대 7", slow.Dropped)
	}
}
```

- [ ] **Step 3: README 를 갱신한다**

`README.md` 를 먼저 **읽고**, 주변 문장과 어조를 살려 고쳐라.

**(a) 지원 범위** — 숫자를 고친다:
- REST **304개**(국내 183 · 미국 121) + **실시간 23** + **조건검색 8** = **335개**
- 아직 아닌 것: OAuth 접근토큰 폐기 **1개**(토큰은 만료로 정리된다)
- 337 − 335 − 1 = 1 이 접근토큰 **발급**이고, `internal/auth` 가 이미 손으로 다룬다

**(b) 실시간 사용 예제** 절을 새로 더한다. 실제로 존재하는 이름을 써라(`grep` 으로 확인):

````markdown
## 실시간 구독

```go
c, _ := kiwoom.NewClientFromEnv()
ctx, cancel := context.WithCancel(context.Background())
defer cancel() // 취소하면 등록을 해제하고 채널을 닫는다

ch, err := c.DomesticRealtime.SubscribeDomesticStockTrade(ctx, "005930")
if err != nil {
    return err
}
for ev := range ch {
    var gap *kiwoom.GapError
    if errors.As(ev.Err, &gap) {
        // 끊겼다 붙었다. 그 사이 이벤트는 받지 못했다 — 필요하면 REST 로 메꿔라.
        log.Printf("실시간 구멍: %s ~ %s", gap.Since, gap.Until)
        continue
    }
    if ev.Err != nil {
        return ev.Err
    }
    fmt.Println(ev.Symbol, ev.Value.CurrentPrice, ev.Value.TradeTime)
}
```

연결은 시장마다 하나다(국내·미국). 여러 구독이 한 소켓에 실리고, 멀티플렉싱은 감춰져 있다.

**구멍을 숨기지 않는다.** 끊기면 자동으로 다시 붙고 등록을 다시 보내지만, 그 사이 이벤트는
영영 받지 못한다. 그것을 `*kiwoom.GapError` 로 알린다 — 메꿀지 말지는 호출자가 정한다.

**모르는 FID 를 버리지 않는다.** 키움이 필드를 추가했는데 이 라이브러리의 표가 아직
모르면 `ev.Raw["1234"]` 로 꺼낼 수 있다. 표를 갱신하기 전까지 데이터가 사라지지 않는다.
````

**(c) 재시도 절에 한 문단을 더한다** — 실시간이 예외로 보이지 않게:

```markdown
실시간 WebSocket 은 끊기면 다시 붙고 등록을 다시 보낸다. 이것은 위 규칙의 예외가 아니다 —
실시간은 **받기만 하는 쪽**이라 재연결이 주문을 두 번 넣지 않는다. 조건검색 요청은
요청/응답이므로 REST 와 같이 **재전송하지 않는다.**
```

**(d) 코드 생성 절**에 FID 표를 덧붙인다:

```markdown
`tools/gen/fids.go` 는 **손으로 적는 표**다. 실시간 값의 식별자는 FID 숫자이고 스펙에
영문명이 없다. 표에 없는 FID 를 만나면 생성기가 멈춘다. 이름을 고칠 때는 **생성물이 아니라
이 표를** 고치고 다시 생성하라.
```

- [ ] **Step 4: 전체 검증**

```bash
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go
go build ./... && go vet ./... && go test ./... -count=1 -race && gofmt -l .
cd tools && go build ./... && go vet ./... && go test ./... -count=1 && gofmt -l .
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go
go list ./... | grep tools && echo "❌ tools 포함됨" || echo "✅ tools 제외됨"
grep -n '304개\|77개' README.md || echo "옛 숫자 없음"
git status --porcelain
```

- [ ] **Step 5: 커밋**

```bash
cd /Users/frankoh/src/workspace_moneyflow/kiwoom-go
git add stream_test.go
git commit -m "test: GapError·SlowConsumerError 가 외부에서 구분되는지" -- stream_test.go
git add README.md
git commit -m "docs: README — 3단계 실시간 구독과 범위 갱신" -- README.md
```

**푸시하지 마라. PR 도 만들지 마라** — 컨트롤러가 한다.

---

## 자체 검토 메모

**설계 절 → 작업 대응**

| 설계 | 작업 |
|---|---|
| §2 범위(31개) | T9 Step 5 의 기대 개수표 |
| §3 전송·연결 둘 | T2(연결·로그인·PING), T5(URL 옵션) |
| §3 핸드셰이크 | T1(기록), T2(구현), T2 Step 3(테스트) |
| §4 타입별 채널·`Event` | T5(`stream.go`), T8(실시간 템플릿) |
| §5 재연결·`GapError` | T4 |
| §6 FID 표 400개 | T6(초안), T7(검토) |
| §7 `values` 맵·`Raw` | T8 Step 4 의 두 번 풀기, T10 Step 5 테스트 |
| §8 조건검색·`ka10173`·`usa20290` | T9(생성 6개), T10(손으로 2개) |
| §9 생성기 `Kind`·`Clients` 29 | T8, T9 |
| §10 검증 | T2~T4 가짜 서버, T6 완전성, T9 골든, T11 외부 모듈 |
| §11 위험 | T6/T9 가 표에 없는 FID 에서 멈춤, T4 가 구멍 신호 |
| §12 커밋 분리 | T1·T2·T3·T4·T5·T6·T8·T9(3개)·T10·T11(2개) |

**의도적으로 이렇게 한 것**

- **T5 가 컴파일이 깨진 상태로 커밋된다.** 루트가 `newClients(tr, dom, ovs)` 를 부르는데
  생성물은 T9 에서 온다. 나누지 않으면 T5~T9 가 한 덩어리가 되어 검토가 불가능해진다.
  T5 Step 4 가 이것을 명시적으로 확인하고, T9 Step 8 이 닫는다.
- **`Event` 가 루트와 각 WS 패키지에 같은 모양으로 두 번 있다.** 루트가 하위 패키지를
  import 하므로(subclients.go) 하위가 루트를 import 할 수 없다. 별칭이 아니라 같은 정의다.
- **`ka10173` 의 푸시를 `841`(일련번호)로 라우팅한다.** 조건검색 푸시는 실시간처럼
  type+item 으로 갈리지 않는다. 이 FID 가 조건검색식 일련번호다.
- **T7(FID 검토)이 별도 작업이다.** 설계 §6 이 "검토를 구현과 분리한다"고 정했다.
  400개는 구현자가 방금 쓴 눈으로 보면 안 보인다.

**작업자가 막히기 쉬운 곳**

- `BuildTree` 는 `ka10173` 에서 **에러를 낸다**(`Object`·`List<Map>`). T9 Step 4 의
  `HandWritten` 검사가 `BuildTree` 호출 **앞**에 와야 한다.
- `tools/` 는 별도 모듈이다. `go test ./...` 를 루트에서 돌리면 생성기 테스트가 안 돈다.
- 타이밍이 있는 테스트는 `-count=1` 로 돌린다. 캐시된 PASS 를 믿으면 안 된다.
